// List rendering now lives in ./layout.tsx (shared with [customerId]/page.tsx
// so the list never remounts when a thread opens/closes on desktop).
export default function ChatPage() {
  return null;
}
