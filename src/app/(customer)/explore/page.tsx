import { ExploreScreen } from "../_components/ExploreScreen";

/** The signed-in customer's home. Guests get the same screen at `/`. */
export default function ExplorePage() {
  return <ExploreScreen />;
}
