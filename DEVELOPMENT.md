# QueueFlow — ডেভেলপমেন্ট গাইড (একমাত্র সোর্স অব ট্রুথ)

> **Claude Code-এর জন্য:** এই ফাইলটাই সম্পূর্ণ ডেভেলপমেন্ট প্ল্যান। প্রতিটা স্প্রিন্ট আলাদাভাবে ইমপ্লিমেন্ট হবে —
> এক স্প্রিন্ট শেষ ও ভেরিফাই না হলে পরেরটা শুরু নয়। কাজ শেষে এই ফাইলের চেকবক্স `[x]` করে আপডেট করবে।
> প্রজেক্ট পরিচিতি `README.md`-তে।

---

## ১. প্রজেক্ট কী ও কোথায় আছি

**QueueFlow (পালা)** — সেলুন/পার্লারের লাইভ সিরিয়াল ম্যানেজমেন্ট। কাস্টমার আশেপাশের দোকান দেখে অনলাইনে সিরিয়াল নেয়, লাইভ ETA ট্র্যাক করে; দোকানদার লাইভ কিউ বোর্ডে সিরিয়াল চালায় ও ব্যবসার হিসাব দেখে।

**গুরুত্বপূর্ণ:** এটা appointment/টাইম-স্লট অ্যাপ **না** — এটা **লাইভ কিউ** প্রোডাক্ট। রেফারেন্স ডিজাইন (Signature Salon UI kit) থেকে আমরা শুধু কালার/UI-UX প্যাটার্ন আর সাপোর্টিং ফিচার নিচ্ছি; কোর বুকিং মডেল কিউ ইঞ্জিনই থাকবে।

### আর্কিটেকচার (সংক্ষেপে)

- **স্ট্যাক:** Next.js App Router + React + Tailwind; Supabase (Auth / Postgres / Realtime / Storage); Leaflet + OSM ম্যাপ
- **রোল:** `profiles.role` = customer | provider — রেজিস্ট্রেশনে ঠিক হয়, রোল-ভিত্তিক রাউটিং (`(customer)` / `(provider)` রুট গ্রুপ)
- **মূল টেবিল:** `shops` (business_type SALON/PARLOUR, lat/lng, is_open), `services`, `chairs` (স্টাফ/চেয়ার), `serials` (বুকিং: status WAITING/IN_PROGRESS/DONE/CANCELLED/NO_SHOW, position, estimated_start_at), `reviews`, `queue_public` ভিউ
- **কিউ লজিক সার্ভার-সাইডে (Postgres ফাংশন):** `assign_best_chair`, `estimate_duration_on_chair`, `chair_backlog_min` — ETA হিসাব সব ক্লায়েন্টে কনসিস্টেন্ট
- **রিয়েলটাইম:** Supabase Realtime (`src/lib/supabase/realtime.ts`) — কিউ বোর্ড ও লাইভ ট্র্যাকিং রিফ্রেশ ছাড়া আপডেট হয়
- **কোড অর্গানাইজেশন:** ফিচার-ভিত্তিক — `src/features/<feature>/{components,hooks,api,lib,schemas}`; শেয়ার্ড UI `src/components/ui/`

### যা ইতিমধ্যে সম্পূর্ণ ✅ (ভাঙা যাবে না)

রোল-ভিত্তিক auth · দোকান সেটআপ (Open/Closed টগল, ম্যাপে লোকেশন পিক, ছবি) · চেয়ার/স্টাফ ও সার্ভিস ম্যানেজমেন্ট · Explore লিস্ট+ম্যাপ (লাইভ queue count) · সার্ভিস মাল্টি-সিলেক্ট বুকিং + best-chair অটো-অ্যাসাইন + one-active-serial rule · লাইভ সিরিয়াল ট্র্যাকিং (ETA কাউন্টডাউন) · প্রোভাইডার কিউ বোর্ড (চেয়ার-কলাম, Start/Done, walk-in) · ইনকাম ট্র্যাকিং · অ্যানালিটিক্স (peak hours/days) · নিয়মিত কাস্টমার + রিমাইন্ডার · রিভিউ (স্টার+টেক্সট) · Trust Score + কাস্টমার প্রোফাইল/খরচ ড্যাশবোর্ড · অ্যাডভান্স পেমেন্ট (mock gateway, সোয়াপযোগ্য ইন্টারফেস `src/lib/payment/`) · বুকিং-স্কোপড চ্যাট · রেসপনসিভ নেভ শেল

---

## ২. কনফার্মড সিদ্ধান্ত (সব স্প্রিন্টে প্রযোজ্য)

1. **লেআউট রিইউজ, স্টাইল সোয়াপ:** বিদ্যমান লেআউট স্ট্রাকচার/কম্পোনেন্ট অক্ষত; শুধু **কালার টোকেন + কম্পোনেন্ট স্টাইল** Signature-স্টাইলে (লাল/কোরাল #E85B5B ঘরানা, সাদা কার্ড, নরম শ্যাডো, ফুল-পিল বাটন) বদলাবে। নতুন স্ক্রিনে Signature-এর প্যাটার্ন: বটম-শিট, চিপ পিকার, হরাইজন্টাল ট্যাব বার, হিরো হেডার + ফ্লোটিং রেটিং ব্যাজ, স্টিকি বটম CTA।
2. **প্রোভাইডার UI:** সাইডবার লেআউট থাকবে, কিন্তু সাদা/লাইট থিমে (বাকি অ্যাপের সাথে সামঞ্জস্যপূর্ণ) — accent Signature-রেডে; ডার্ক chrome আর না (Sprint 1-এ ছিল, পরে বাদ দেওয়া হয়েছে)।
3. **পেমেন্ট:** রিয়েল গেটওয়ে **এখন না** — Payment Methods UI-তে Cash/bKash/Nagad/Rocket/Card অপশন থাকবে (অনলাইনগুলো "শীঘ্রই আসছে"), mock ইন্টারফেস অক্ষত, পরে গেটওয়ে সোয়াপ।
4. **ভাষা (Sprint 10 থেকে বাধ্যতামূলক নিয়ম — ভবিষ্যতের সব ফিচারে প্রযোজ্য):** অ্যাপে হালকা কাস্টম bilingual সিস্টেম আছে (`src/lib/i18n/`, কোনো heavy dependency ছাড়া) — ডিফল্ট বাংলা, টগলে ইংরেজি, পছন্দ `localStorage`-এ persist। **নতুন যেকোনো UI টেক্সট এই প্যাটার্ন ছাড়া লেখা যাবে না:**
   - প্রতিটা ফিচারের নিজস্ব `lib/i18n.ts`-এ `{bn, en}` key যোগ করো (`satisfies Dict`); কম্পোনেন্টে `useT(dict)` দিয়ে পড়ো। নতুন ফিচার হলে নতুন dict বানাও।
   - কোনো স্ট্রিং প্যারামিটার নিলে entry-টা ফাংশন করো: `{ bn: (n) => \`${n}টা\`, en: (n) => \`${n} items\` }`।
   - কম্পোনেন্টের বাইরে (api.ts/hook/module-scope helper) স্ট্রিং লাগলে `translate(dict, "key", ...)` — `useT()` না, ওটা হুক।
   - নতুন zod ভ্যালিডেশন স্কিমা static const না, factory ফাংশন: `export function xSchema(lang: Language) {...}`, কমন মেসেজ `resolveDict(VALIDATION, lang, "key")` (`src/lib/i18n/validation-messages.ts`); ফর্মে `useMemo(() => xSchema(language), [language])`।
   - কখনোই কোনো translate() করা টেক্সট module-scope static array/object-এ বেক করে রাখবে না (import-time ভাষায় আটকে যায়, টগলে বদলায় না) — component-এর ভেতরে বানাও বা lazily resolve করো।
   - ইউজার-জেনারেটেড কন্টেন্ট (রিভিউ কমেন্ট, অফার-টাইটেল, চ্যাট মেসেজ) আর DB-ট্রিগার-জেনারেটেড নোটিফিকেশন টেক্সট অনুবাদ করার দরকার নেই — শুধু অ্যাপের নিজস্ব UI কপি bilingual হবে।
   - পুরো আর্কিটেকচার+উদাহরণ Sprint 10-এর নোটে (নিচে) ও memory-তে ([[project_palaa_i18n_architecture]]) আছে।
5. **প্রতি স্প্রিন্টে বাধ্যতামূলক:** মোবাইল-ফার্স্ট রেসপনসিভ (ছোট ফোন → ট্যাবলেট → ডেস্কটপ) · ইউজার-ফ্রেন্ডলি (লোডিং/এম্পটি/এরর স্টেট সহ) · বিদ্যমান ফিচার রিগ্রেশন নয় · নতুন টেবিলে মাইগ্রেশন (`supabase/migrations/`) + RLS পলিসি · `npm run build` ও lint সবুজ করে কমিট · এই ফাইলের চেকবক্স আপডেট।
6. **স্ট্যাক বদলানো যাবে না;** ভারী নতুন ডিপেন্ডেন্সি এড়ানো হবে।
7. **বাদ (ব্যাকলগে):** সোশ্যাল ফলোয়ার লেয়ার, প্যাকেজ/বান্ডেল, social login, ইন-অ্যাপ টার্ন-বাই-টার্ন রুট/geofence, PayPal/Apple Pay।

### ২.১ ফেজ ২ সিদ্ধান্ত (Sprint 12 থেকে প্রযোজ্য)

Sprint 1–11 শেষ হওয়ার পর ফেজ ২-এর স্কোপ ঠিক করার সময় নেওয়া সিদ্ধান্ত। **Sprint 12+ এর সব কাজে এগুলো বাধ্যতামূলক:**

8. **মোবাইল-ফার্স্ট, শুধু "রেসপনসিভ" না:** এই সিস্টেম বেশিরভাগ **মোবাইলে** চলবে। তাই ৩২০px ছোট ফোনই ডিজাইনের বেসলাইন — ডেস্কটপ তার এনহ্যান্সমেন্ট, উল্টোটা নয়। প্রতিটা স্ক্রিন ৩২০px-এ হরাইজন্টাল স্ক্রল ছাড়া, কোনো ক্লিপড/ওভারল্যাপড এলিমেন্ট ছাড়া কাজ করতে হবে; মিনিমাম টাচ-টার্গেট ৪৪px।
9. **অথ (চূড়ান্ত):** সাইনআপে **ইমেইলে ভেরিফিকেশন কোড** (SMS খরচ এড়াতে) · **মোবাইল নম্বর বাধ্যতামূলক ফিল্ড** কিন্তু ভেরিফাই করা হয় না (কল/WhatsApp/রিমাইন্ডারে কাজে লাগে) · **প্রতিবার লগইনে কোড লাগবে না** — শুধু সাইনআপে একবার। SMS-ভিত্তিক ফোন ভেরিফিকেশন ব্যাকলগে।
10. **সেলুন আগে, পার্লার পরে:** রেজিস্ট্রেশনেই দোকানদার সেলুন/পার্লার বাছবে ও `shops.business_type`-এ সেভ হবে, কিন্তু **এখন শুধু সেলুন ফ্লো সম্পূর্ণ করা হবে**। পার্লার-স্পেসিফিক সেটআপ ব্যাকলগে — পরে সেলুনের অনেক কিছু রিইউজ হবে।
11. **পেমেন্ট মেথডের দুই অর্থ আলাদা:** দোকানদারের সাইডে = "আমি কোন কোন পেমেন্ট **নিই**" (শপ সেটিংসের অংশ, সাইডবারের আলাদা পেজ না) → এই লিস্টই কাজ সম্পন্ন করার পেমেন্ট শিটে দেখাবে। কাস্টমারের সাইডে = "আমি কী দিয়ে **পে করবো**" (পরে কাস্টমার স্প্রিন্টে)।
12. **অফার থাকবে:** যে দোকান অফার দিতে চায় দেবে, না চাইলে নাই — ফিচার হাইড/রিমুভ করা হবে না, বরং এডিট/আপডেট/ডিলিট সম্পূর্ণ করা হবে।
13. **আয়ের অঙ্ক ডিফল্টে ঢাকা:** সেলুনে দোকানদারের ফোন প্রায়ই কাস্টমারের চোখের সামনে থাকে — সাইড প্যানেলের আয় ট্যাপ-টু-রিভিল (ব্যাংকিং অ্যাপ স্টাইল)।
14. **শুধু মেসেজ:** ইন-অ্যাপ কল/ভিডিও কল স্কোপে নেই। বাইরের যোগাযোগ শুধু `tel:` ও `wa.me` ডিপ-লিংক দিয়ে।

---

## ৩. স্প্রিন্ট প্ল্যান

### Sprint 1 — Signature-রেড রি-স্কিন

**লক্ষ্য:** পুরো অ্যাপের ভিজ্যুয়াল টোন Palaa-warm থেকে Signature-রেডে — লেআউট না ভেঙে।

- [x] `globals.css` টোকেন সোয়াপ: `--accent` → কোরাল-রেড স্কেল (hover/pressed শেডসহ), `--paper` → হালকা অফ-হোয়াইট, `--card` → সাদা; স্ট্যাটাস রং (`--live`, `--good`...) নতুন প্যালেটে সামঞ্জস্যপূর্ণ; টেক্সট কনট্রাস্ট AA
- [x] Button/Badge/Pill: ফুল-রাউন্ডেড, প্রাইমারি = লাল ফিল + সাদা টেক্সট, সেকেন্ডারি = লাল আউটলাইন
- [x] Card: সাদা সারফেস, নরম শ্যাডো, বড় রেডিয়াস
- [x] কাস্টমার বটম নেভ + প্রোভাইডার সাইডবার active state লাল accent (সাইডবার সাদা/লাইট থিমে, ডার্ক chrome বাদ)
- [x] সব স্ক্রিন ঘুরে হার্ডকোডেড পুরনো রং খুঁজে টোকেনে আনা

**Acceptance:** কোথাও পুরনো Palaa accent নেই; লেআউট/স্পেসিং অপরিবর্তিত; মোবাইল+ডেস্কটপ দুটোতেই যাচাই।

### Sprint 2 — নোটিফিকেশন সিস্টেম

**লক্ষ্য:** in-app Notification Center + রিয়েলটাইম + দোকানদারের ব্রডকাস্ট। (Web Push পরে — আগে in-app।)

- [x] মাইগ্রেশন: `notifications` (user_id, type: SERIAL_CONFIRMED/QUEUE_UPDATE/YOUR_TURN/CANCELLED/PROMO/REMINDER, title, body, data jsonb, read_at, created_at) + RLS
- [x] সিরিয়াল ইভেন্টে অটো-নোটিফিকেশন: বুকিং কনফার্ম ("তোমার সিরিয়াল #N"), আগে ২ জন বাকি, "এখন তোমার পালা", ক্যানসেল
- [x] কাস্টমার UI: হেডারে বেল + unread ব্যাজ; Notification Center — ডেট-গ্রুপড লিস্ট (আজ/গতকাল/তারিখ), read মার্ক, সব-read; Realtime-এ লাইভ
- [x] প্রোভাইডার ব্রডকাস্ট: "নোটিফিকেশন পাঠান" — টার্গেট (আগের কাস্টমার/regulars), টাইটেল+মেসেজ; দৈনিক রেট-লিমিট
- [x] regulars রিমাইন্ডার এই সিস্টেমে যুক্ত

**Acceptance:** দুই ব্রাউজারে (কাস্টমার+প্রোভাইডার) রিফ্রেশ ছাড়া নোটিফিকেশন পৌঁছায়; ব্রডকাস্ট কাজ করে।

### Sprint 3 — Auth + অনবোর্ডিং সম্পূর্ণ

- [x] Forgot Password: লগইনে লিংক → Supabase reset ইমেইল → নতুন পাসওয়ার্ড পেজ
- [x] Password Manager: সেটিংসে পাসওয়ার্ড বদল (বর্তমান/নতুন/কনফার্ম)
- [x] সাইনআপ-পরবর্তী Complete Profile ধাপ: ছবি, ফোন, জেন্ডার (skip-able)
- [x] Location: explore-এ permission প্রম্পট + ম্যানুয়াল লোকেশন অপশন; অনুমতি পেলে **দূরত্ব-সর্ট + শপ কার্ডে দূরত্ব**
- [x] Logout কনফার্মেশন বটম-শিট

### Sprint 4 — শপ ডিটেইল ২.০

- [x] ট্যাবড লেআউট: **সার্ভিস / স্টাফ / গ্যালারি / রিভিউ / বিস্তারিত** (হরাইজন্টাল স্ক্রল ট্যাব); বুকিং ফ্লো (মাল্টি-সিলেক্ট + স্টিকি CTA) অক্ষত
- [x] হিরো কভার + ফ্লোটিং রেটিং ব্যাজ (★ গড় + সংখ্যা)
- [x] Quick Actions: কল (tel:) · মেসেজ (চ্যাট) · ডিরেকশন (ম্যাপ লিংক lat,lng) · শেয়ার (Web Share API, fallback কপি)
- [x] স্টাফ ট্যাব: চেয়ার/স্টাফ কার্ড (ছবি, নাম, পারদর্শিতা)
- [x] গ্যালারি: ফটো গ্রিড; দোকানদার সেটিংসে মাল্টি-আপলোড (Supabase Storage)
- [x] বিস্তারিত: About টেক্সট + সাপ্তাহিক Working Hours টেবিল — মাইগ্রেশন: shops-এ `about`, `weekly_hours` jsonb; সেটিংসে এডিট UI
- [x] বুকিংয়ে **পছন্দের স্টাফ (optional)** — auto-assign ডিফল্ট, বাছলে সেই চেয়ারের ব্যাকলগে ETA

### Sprint 5 — বুকিং হিস্ট্রি + E-Receipt + Favorites

- [x] "আমার বুকিং" ৩-ট্যাব: **চলমান / সম্পন্ন / বাতিল** (কার্ড: তারিখ-সময়, দোকান, ঠিকানা, সিরিয়াল কোড)
- [x] বাতিলে **আবার বুক করুন** (same shop+services → নতুন সিরিয়াল ফ্লো)
- [x] সম্পন্নে **E-Receipt**: QR (serial code), আইটেমাইজড সার্ভিস+দাম, টোটাল, ডাউনলোড (client-side)
- [x] কাস্টমার Transactions: ডেট-গ্রুপড খরচ (বিদ্যমান spending ডেটা রিইউজ)
- [x] Favorites: ♥ টগল (কার্ড+ডিটেইল); মাইগ্রেশন `favorites`; প্রোফাইলে "প্রিয় দোকান" (ক্যাটাগরি চিপ)

### Sprint 6 — হোম রিচনেস + অফার + ফিল্টার/সার্চ

- [x] মাইগ্রেশন: `offers` (shop_id, title, description, discount_pct, valid_until, active)
- [x] দোকানদার অফার পেজ: তৈরি/বন্ধ; তৈরি হলে regulars-দের নোটিফিকেশন (Sprint 2 রিইউজ)
- [x] হোম/explore-এ **অফার ব্যানার ক্যারোসেল** (ডার্ক কার্ড, ডিসকাউন্ট, CTA → শপ ডিটেইল)
- [x] সার্ভিস-ক্যাটাগরি শর্টকাট রো (গোল আইকন) → ফিল্টারড লিস্ট
- [x] টপ-রেটেড সেকশন (গড় রেটিং সর্ট)
- [x] সার্চ (দোকান/সার্ভিস নাম) + ফিল্টার বটম-শিট: ধরন (সেলুন/পার্লার), রেটিং রেঞ্জ, দূরত্ব স্লাইডার; Reset+Apply
  (নোট: "খোলা-শুধু" ফিল্টার বাদ দেওয়া হয়েছে — ইউজারের সিদ্ধান্তে, কারণ RLS অনুযায়ী কাস্টমার সাইডে বন্ধ দোকান এমনিতেই দেখা যায় না)

### Sprint 7 — চ্যাট ২.০ + রিভিউ ২.০ ✅

- [x] Chats লিস্ট (দুই সাইডে): সব কনভার্সেশন — অ্যাভাটার, নাম, শেষ মেসেজ, সময়, unread ডট
- [x] থ্রেডে ছবি পাঠানো (Storage) + online/last-seen (Supabase Presence)
- [x] রিভিউয়ে ছবি (একাধিক); শপ রিভিউ ট্যাবে ফিল্টার: সর্বশেষ/ছবিসহ; verified ব্যাজ (সার্ভিস নেওয়া কাস্টমার)
- [x] রিভিউয়ে স্টাফ অটো-ট্যাগ (serial থেকে) → স্টাফ কার্ডে per-staff রেটিং
  - (নোট: "online/last-seen" আসল Presence দিয়ে করা হয়েছে, কিন্তু কিছুই persist হয় না — তাই দুই পক্ষই অফলাইন হয়ে গেলে "last seen" এর বদলে থ্রেডের শেষ মেসেজের সময় দেখানো হয়, approximate।)

### Sprint 8 — বাকি খাতা + পেমেন্ট অপশন (গেটওয়ে ছাড়া) ✅

- [x] মাইগ্রেশন: payment স্ট্যাটাস (PAID/DUE/ADVANCE) + due ট্র্যাকিং
- [x] প্রোভাইডার **বাকির খাতা**: কাস্টমার-প্রতি বাকি ব্যালেন্স; "Done"-এর সময় "বাকি রাখলো" অপশন; "আদায় হয়েছে" মার্ক → তখন ইনকামে যোগ
- [x] বাকি রিমাইন্ডার নোটিফিকেশন বাটন
- [x] Payment Methods স্ক্রিন: Cash (ডিফল্ট) / bKash / Nagad / Rocket / কার্ড — UI only, অনলাইনে "শীঘ্রই আসছে"; mock ইন্টারফেস অক্ষত
  - (নোট: নতুন বুকিং ডিফল্টে payment_status='DUE' — provider "✓ কাজ সম্পন্ন" চাপলে সাথে সাথে PAID+ইনকামে যোগ হয়, "বাকি রেখে সম্পন্ন করো" চাপলে DUE থেকে যায় যতক্ষণ না বাকির খাতা থেকে "আদায় হয়েছে" মার্ক করা হয়। পুরনো (migration-পূর্ব) সব DONE সিরিয়াল ব্যাকফিল করে PAID করা হয়েছে যাতে ইনকাম হিস্ট্রি ভেঙে না যায়।)

### Sprint 9 — সাপোর্ট পেজ + পলিশ ✅

- [x] Help Center: FAQ অ্যাকর্ডিয়ন (সার্চ + ক্যাটাগরি চিপ) + যোগাযোগ (ফোন/WhatsApp/সোশ্যাল)
- [x] Privacy Policy + Terms + Cancellation Policy স্ট্যাটিক পেজ
- [x] Delete Account ফ্লো (কনফার্মেশন + ডেটা হ্যান্ডলিং)
- [x] Notification Settings (টাইপ-ভিত্তিক অন/অফ)
- [x] ফুল রেসপনসিভ QA (মোবাইল/ট্যাবলেট/ডেস্কটপ), সব এম্পটি/লোডিং/এরর স্টেট, অ্যানিমেশন পলিশ
  - (নোট: Notification প্রেফারেন্স সত্যিকারের DB-লেভেলে এনফোর্সড — প্রতিটা notify ফাংশন (`notify_serial_event`, `broadcast_shop_notification`, `notify_regular_reminder`, `send_due_reminder`) এখন `notification_enabled()` হেল্পার দিয়ে গেটেড, শুধু UI টগল না। বুকিং কনফার্মেশন সবসময় চালু থাকে, বাকি ৫ টাইপ ইচ্ছেমতো বন্ধ রাখা যায়। Delete Account নিজের প্রোফাইল/চ্যাট/রিভিউ/ফেভারিট/নোটিফিকেশন মুছে ফেলে (cascade); যে প্রোভাইডারের নামে দোকান আছে তাকে ব্লক করে সাপোর্টে পাঠানো হয় — পুরো দোকান-টিয়ারডাউন এই স্প্রিন্টের স্কোপে নেই।)

### Sprint 10 — ভাষা টগল (বাংলা/ইংরেজি) ✅

- [x] সব হার্ডকোডেড বাংলা স্ট্রিং কেন্দ্রীয় ডিকশনারিতে রিফ্যাক্টর
- [x] ইংরেজি অনুবাদ + সেটিংসে ভাষা সিলেক্টর; ডিফল্ট বাংলা; প্রেফারেন্স persist
  - (নোট: হালকা কাস্টম i18n (`src/lib/i18n/`) — কোনো নতুন heavy dependency ছাড়া। প্রতিটা ফিচারের নিজস্ব `lib/i18n.ts` ডিকশনারি (`{bn, en}` per key), শেয়ার্ড `useT()`/`translate()`/`resolveDict()` হেল্পার। ভাষা পছন্দ `localStorage`-এ persist হয় (ডিভাইস-লোকাল, লগইন-নির্ভর না)। Zod স্কিমাগুলো factory function-এ রূপান্তরিত হয়েছে (`xSchema(lang)`) যাতে ভ্যালিডেশন মেসেজও ভাষা-সচেতন থাকে। `format-wait.ts`/`weekly-hours.ts`/`date-groups.ts` এখন ভাষা-সচেতন internally, তাই কল-সাইট বদলাতে হয়নি। ইউজার-জেনারেটেড কন্টেন্ট (রিভিউ কমেন্ট, অফার টাইটেল, চ্যাট মেসেজ, DB ট্রিগার-জেনারেটেড নোটিফিকেশন টেক্সট) ইচ্ছাকৃতভাবে অনুবাদ করা হয়নি — শুধু অ্যাপের নিজস্ব UI কপি।)

### Sprint 11 — টেস্টিং ও হার্ডেনিং ✅

- [x] ETA ক্যালকুলেশন ইউনিট টেস্ট (serial N wait = current remaining + Σ আগেরদের service minutes)
- [x] one-active-serial এনফোর্সমেন্ট টেস্ট
- [x] কী-ফ্লো: বুকিং → লাইভ ট্র্যাকিং → Done → ইনকাম/নোটিফিকেশন আপডেট
- [x] RLS রিভিউ (নতুন টেবিলসহ), এজ-কেস (দোকান বন্ধ হলে active serial, ক্যানসেল রেস)
  - (নোট: **বড় আবিষ্কার** — পুরো কিউ ইঞ্জিন, `serial_before_insert`/`serial_before_update`/`serial_after_insert`/`serial_after_update`/`sync_queue_public`/`recalc_queue_estimates`/`assign_best_chair`/`estimate_duration_on_chair`/`chair_backlog_min`/`is_shop_owner`/`enforce_provider_owner`/`lock_profile_role` ট্রিগার-ফাংশন, `one_active_serial_per_customer`/`one_in_progress_per_chair` কনস্ট্রেইন্ট, আর `shops`/`services`/`chairs`/`chair_service_stats`/`serials`/`profiles`-এর RLS পলিসি — এসবের কোনো SQL এই রিপোর কোনো migration-এ ছিলই না (লাইভ Supabase ড্যাশবোর্ডে সরাসরি বসানো, রিপোর হিস্ট্রির আগে থেকেই)। Read-only SQL দিয়ে লাইভ প্রজেক্ট থেকে সব ফাংশন/ট্রিগার/কনস্ট্রেইন্ট/পলিসি বের করে **`supabase/migrations/20260730_capture_baseline_queue_engine.sql`**-এ idempotent বেসলাইন হিসেবে ক্যাপচার করা হয়েছে (আচরণ বদলায় না, শুধু ভার্শন-কন্ট্রোলে আনে)।
  - **`recalc_queue_estimates(chair_id)`**-ই আসল ETA ফর্মুলা: চেয়ারের WAITING/IN_PROGRESS রো position-অনুযায়ী হেঁটে, IN_PROGRESS জবের বাকি সময় থেকে কার্সার শুরু করে প্রতিটা WAITING রো-তে `estimated_start_at = কার্সার` বসায়, তারপর কার্সার ওই রো-র duration দিয়ে এগোয় — ঠিক "বাকি + আগেরদের যোগফল" সূত্র। যেহেতু লোকাল Postgres/CLI নেই, এই SQL ফাংশনের সরাসরি ইউনিট টেস্ট সম্ভব না — তার বদলে ক্লায়েন্ট-সাইড প্রতিফলন (`src/lib/queue-wait.ts`-এর `chairFreeAtMs`/`minutesUntil`, যেগুলো `estimated_start_at`-ই কনজিউম করে) আর one-active-serial-এর error-contract (`src/lib/supabase/db-errors.ts`-এর `translateDbError`) Vitest দিয়ে ইউনিট-টেস্ট করা হয়েছে (নতুন dev dependency, "ভারী নতুন ডিপেন্ডেন্সি এড়ানো"-র সচেতন ব্যতিক্রম) — মোট ৫টা টেস্ট ফাইল, ৪৯টা টেস্ট (queue-wait, db-errors, provider-income/customer-profile/provider-analytics-এর compute-* ফাংশন)। `npm test` স্ক্রিপ্ট যোগ হয়েছে।
  - **RLS রিভিউয়ে একটা আসল বাগ পাওয়া গেছে ও ফিক্স করা হয়েছে:** `chairs` টেবিলে দুইটা permissive SELECT পলিসি ছিল — একটা `is_active = true` কন্ডিশনসহ, আরেকটা শর্তহীন (`true`)। Postgres একই কমান্ডের একাধিক permissive পলিসি **OR** দিয়ে মেশায়, তাই শর্তহীন পলিসিটা `is_active` রেস্ট্রিকশনকে অকেজো করে দিচ্ছিল — ইনঅ্যাক্টিভ (সরানো) চেয়ারও পাবলিকলি দেখা যাচ্ছিল। ফিক্স: **`supabase/migrations/20260730_fix_chairs_public_read_rls.sql`** (আলাদা, ছোট migration যেহেতু এটা সত্যিকারের বিহেভিয়ার বদলায়) — বাকি সব পলিসি (shops/services/serials/profiles/chair_service_stats) রিভিউ করে ঠিক পাওয়া গেছে (customer শুধু নিজের সিরিয়াল CANCELLED-এ নিতে পারে, provider শুধু নিজের শপ ম্যানেজ করতে পারে, cross-user/cross-shop কোনো লিক নেই)।
  - **এজ-কেস লাইভ Playwright QA দিয়ে ভেরিফাইড** (দুই টেস্ট অ্যাকাউন্টে, শুধু Karim-এর চেয়ার, Rahim/Md. Rayhan-এর real বুকিং অস্পৃষ্ট): (১) one-active-serial — শপ-স্কোপড ক্লায়েন্ট গার্ড দ্বিতীয় বুকিং আটকায় সুন্দরভাবে ("তোমার এই দোকানে ইতিমধ্যে একটা সিরিয়াল আছে"), DB-লেভেলে গ্লোবাল (সব শপ জুড়ে) পার্শিয়াল ইউনিক ইনডেক্স দিয়ে এনফোর্সড। (২) কী-ফ্লো: বুকিং→লাইভ ট্র্যাকিং→provider Start (নোটিফিকেশন YOUR_TURN+SERIAL_CONFIRMED ফায়ার্ড)→Done→ইনকাম ৳750→৳900 আপডেট, সব কনফার্মড। (৩) শপ বন্ধ এজ-কেস: **বাগ না, ডিজাইন অনুযায়ী** — শপ বন্ধ করলে বিদ্যমান অ্যাক্টিভ সিরিয়াল স্বাভাবিকভাবে চলতে থাকে (লাইভ ট্র্যাকিং/প্রোভাইডার Start-Done সবই কাজ করে), শুধু নতুন বুকিং আটকায়, স্পষ্ট মেসেজসহ ("শপ এখন বন্ধ — এই মুহূর্তে বুকিং করা যাচ্ছে না") — কোনো ফিক্স লাগেনি। (৪) ক্যানসেল রেস: কাস্টমার cancel + provider Start প্রায় একসাথে ফায়ার করে দেখা হয়েছে — realtime দিয়ে ক্লিনভাবে reconcile হয়, কোনো ক্র্যাশ/ঝুলে-থাকা স্টেট/এরর টোস্ট আটকে থাকেনি।)

---

## ৩.১ ফেজ ২ — মোবাইল-ফার্স্ট + ডিজাইন রিফ্রেশ (Sprint 12–23)

> Sprint 1–11-এ ফিচার সম্পূর্ণ হয়েছে। ফেজ ২-এর কাজ **নতুন ফিচার না** — বিদ্যমান ফিচারগুলোকে
> মোবাইলে সত্যিকারের ব্যবহারযোগ্য করা, ডিজাইন রিফ্রেশ করা, আর বাস্তব ব্যবহারে যে ফাঁকগুলো ধরা পড়েছে
> সেগুলো পূরণ করা। **Sprint 12–19 দোকানদার (provider) সাইডের, Sprint 20–23 কাস্টমার সাইডের**
> (ব্যতিক্রম Sprint 16 — মেসেজ, যেটা শেয়ার্ড কম্পোনেন্টের কারণে দুই সাইড একসাথে কভার করে)।
>
> নিয়ম আগের মতোই: এক স্প্রিন্ট শেষ ও ভেরিফাই না হলে পরেরটা শুরু নয়; বিদ্যমান ফিচার ভাঙা যাবে না;
> নতুন সব UI টেক্সট bilingual (সিদ্ধান্ত ৪); `npm run build` ও lint সবুজ করে কমিট।

### Sprint 12 — মোবাইল-ফার্স্ট ফাউন্ডেশন + প্রোভাইডার শেল

**লক্ষ্য:** ভিত্তি স্প্রিন্ট — এরপরের সব স্প্রিন্ট এই টোকেন ও কম্পোনেন্ট প্যাটার্নের উপর দাঁড়াবে। এখানে
শর্টকাট নিলে পরের প্রতিটা স্প্রিন্টে একই রেসপনসিভ বাগ বারবার ফিরে আসবে।

- [x] **রেসপনসিভ ভিত্তি:** ব্রেকপয়েন্ট কনভেনশন ঠিক করা (৩২০px ছোট ফোন → ট্যাবলেট → ডেস্কটপ), মিনিমাম টাচ-টার্গেট ৪৪px, safe-area inset (নচ/হোম-বার), কোনো স্ক্রিনে হরাইজন্টাল ওভারফ্লো নয়
- [x] **টাইপোগ্রাফি ও কনট্রাস্ট টোকেন:** `--muted` টেক্সটের কনট্রাস্ট AA-তে তোলা, হেডিং স্কেল মোবাইলের জন্য রি-টিউন, লম্বা বাংলা লেবেল wrap হয়ে ভাঙার সমস্যা ঠিক করা
- [x] **`StatusPill` শেয়ার্ড কম্পোনেন্ট:** চালু/বন্ধ · ক্যাশ/বাকি · খোলা/বন্ধ — সব স্ট্যাটাস একই ভিজ্যুয়াল ভাষায়, স্পষ্ট হাইলাইটসহ (এখন কোথাও ছোট ডট, কোথাও ফ্যাকাশে টেক্সট)
- [x] **`BottomSheet` শেয়ার্ড কম্পোনেন্ট:** মোবাইলে সেন্টারড ডায়ালগ → বটম শিট, ডেস্কটপে ডায়ালগই থাকবে (ওয়াক-ইন, পেমেন্ট, কনফার্মেশন — সব রিইউজ করবে)
- [x] **`Avatar` শেয়ার্ড কম্পোনেন্ট:** ছবি + fallback ইনিশিয়াল, এক জায়গা থেকে সব সাইজ — স্টাফ/কাস্টমার/রিভিউ সব জায়গায় রিইউজ
- [x] **সাইড প্যানেল রিডিজাইন:**
  - দোকানের নামের নিচে **খোলা/বন্ধ টগল সুইচ** — যেকোনো পেজ থেকে এক ট্যাপে (সেটিংসে যেতে হবে না)
  - উপরে **`বাং | Eng` কুইক ভাষা টগল** (সেটিংসের বড় সিলেক্টরও থাকবে)
  - "আজকের আয়" কার্ড → **ট্যাপ-টু-রিভিল** (ডিফল্টে ঢাকা, সিদ্ধান্ত ১৩)
  - নিচের অ্যাভাটার/সাইন-আউট রো-র ওভারল্যাপ ফিক্স ("একাউন্ট" লেখা এখন অ্যাভাটারের নিচে ঢাকা পড়ে)

**Acceptance:** ৩২০px-এ প্রতিটা প্রোভাইডার পেজ হরাইজন্টাল স্ক্রল ছাড়া খোলে, কোনো টেক্সট ক্লিপড/ওভারল্যাপড না; সাইড প্যানেল থেকে শপ খোলা-বন্ধ ও ভাষা বদল কাজ করে; আয় ডিফল্টে ঢাকা থাকে।

  - (নোট: **ব্রেকপয়েন্ট কনভেনশন** ডকুমেন্ট করা হয়েছে `globals.css`-এর টপ কমেন্টে (কোনো `tailwind.config.*` নেই, Tailwind v4 CSS-first) — ৩২০px বেসলাইন, `sm:`(640) ইন-পেজ শিট/ডায়ালগ ব্রেকপয়েন্ট, `md:`(768) শেল/সাইডবার কোলাপ্স পয়েন্ট। কাস্টমার শেল (`CustomerShell.tsx`) এখনো `lg:`-এ ব্রেক করে — সেটা কাস্টমার-সাইড ফাইল বলে এই স্প্রিন্টে ছোঁয়া হয়নি, Sprint 20-এ ঠিক হবে।
  - **`--color-muted`** `#857e70` → `#6b6355` (সাদা/`--color-card`-এর বিপরীতে কনট্রাস্ট ৪.০ থেকে ৫.৯-এ, `--color-soft`-এর বিপরীতে ৫.২ — দুটোই AA (৪.৫) পাস করে)।
  - **`StatusPill`** (`src/components/ui/StatusPill.tsx`) `Badge.tsx`-এর এক্সপোর্ট করা `BADGE_VARIANTS` টোকেন রিইউজ করে — নতুন করে কালার ডুপ্লিকেট করেনি।
  - **`BottomSheet`** (`src/components/ui/BottomSheet.tsx`) `ConfirmSheet.tsx`-এর রেসপনসিভ শেল জেনারেলাইজ করে বানানো হয়েছে, backdrop-click + Escape-key ক্লোজ যোগ হয়েছে (আগে ছিল না)। `ConfirmSheet` নিজেই এখন `BottomSheet`-এর উপর রিফ্যাক্টর করা — Playwright দিয়ে ভেরিফাইড (desktop: সেন্টারড ডায়ালগ + কাস্টম লেবেল বাটন কাজ করে; mobile: বটম-শিট + ব্যাকড্রপ-ক্লিক-ক্লোজ কাজ করে)। `WalkInDialog`/`AdvancePaymentDialog`/`ReviewDialog`/`EReceiptSheet`/`FilterSheet` ইচ্ছাকৃতভাবে migrate করা হয়নি — সেগুলো নিজ নিজ পরের স্প্রিন্টে (Sprint 14/15/18) হবে।
  - **`Avatar`** নতুন ফাইল না বানিয়ে বিদ্যমান `AvatarChip.tsx`-এ `shape?: "circle" | "rounded"` prop যোগ করা হয়েছে (ডিফল্ট `"rounded"` — বিদ্যমান একমাত্র কনজিউমার, কাস্টমার এক্সপ্লোর পেজের প্রোফাইল-অ্যাভাটার, visually অপরিবর্তিত থাকে)। এই স্প্রিন্টে অ্যাডপশন শুধু `ProviderSidebar.tsx`-এ (শপ-লোগো + প্রোফাইল-অ্যাভাটার, `shape="circle"`) — বাকি ~১০টা কাস্টমার-সাইড ডুপ্লিকেট সাইট (`StaffTab`, চ্যাট, রিভিউ, ইত্যাদি) নিজ নিজ পরের স্প্রিন্টে মাইগ্রেট হবে।
  - **সাইড প্যানেল রিডিজাইনের সময় একটা রিয়েল লেআউট বাগ ধরা পড়ে ফিক্স করা হয়েছে:** প্রথম ভার্শনে StatusPill+Switch শপ-নাম রো-র একই লাইনে ডানপাশে বসানো ছিল, যেটা ৩২০px আর এমনকি ১৪৪০px ডেস্কটপেও শপের নাম/ঠিকানা মারাত্মকভাবে ট্রাংকেট করে দিচ্ছিল ("P.." / "M.." Playwright স্ক্রিনশটে ধরা পড়ে) — ফিক্স: StatusPill+Switch এখন নাম-রো-র নিচে নিজস্ব ফুল-উইথ রো-তে, যা নাম/ঠিকানার জন্য পূর্ণ প্রস্থ ছেড়ে দেয় (একই সাথে নেস্টেড button-in-anchor সমস্যাও দূর হয়েছে, যেহেতু Switch আর `Link`-এর ভেতরে নেই)।
  - **`ShopSettingsForm.tsx` ইচ্ছাকৃতভাবে ছোঁয়া হয়নি** — এর নিজস্ব ইনলাইন is_open পিল এখনো আছে, Sprint 19-এ সরানো হবে (DEVELOPMENT.md-এ আগে থেকেই এভাবে প্ল্যান করা)।
  - **যাচাই:** `npm run build`/`lint`/`test` তিনটাই গ্রিন (lint-এর ২ error `design/design_handoff_palaa/`-এর প্রি-এক্সিস্টিং, এই স্প্রিন্টের কোড না)। Playwright দিয়ে প্রোভাইডার টেস্ট অ্যাকাউন্ট দিয়ে লগইন করে ১৪টা প্রোভাইডার পেজ ৩২০px-এ `scrollWidth`/`clientWidth` চেক করা হয়েছে — সবগুলোই ওভারফ্লো-মুক্ত। ডেস্কটপ (১৪৪০px) রিগ্রেশন-চেকও ক্লিন। ইনকাম পেজের ১২-মাসের চার্ট এলাকায় সামান্য ভিজ্যুয়াল টাইটনেস চোখে পড়েছে কিন্তু কোনো প্রকৃত হরাইজন্টাল ওভারফ্লো না — সেই পেজের নিজস্ব রেসপনসিভ কাজ Sprint 17-এর স্কোপ, এখানে ছোঁয়া হয়নি। বাকির-খাতা কার্ডের মোবাইল-ভাঙা সমস্যা (DEVELOPMENT.md Sprint 18-এ নোট করা) খালি-স্টেটে রিপ্রোডিউস হয়নি, আসল ডেটাসহ Sprint 18-এ ফিক্স হবে।)

### Sprint 13 — Auth ২.০ (ইমেইল ভেরিফিকেশন + ব্যবসার ধরন)

**লক্ষ্য:** রেজিস্টার/লগইনের পুরো UI নতুন করা এবং অ্যাকাউন্ট ভেরিফিকেশন চালু করা — **SMS খরচ ছাড়াই** (সিদ্ধান্ত ৯)।

- [x] **রেজিস্টার/লগইন পেজের পুরো UI/UX রিডিজাইন** — মোবাইল-ফার্স্ট, নতুন লুক (দুই পেজেই)
- [x] **দোকানদার সাইনআপে ব্যবসার ধরন:** সেলুন / পার্লার চিপ সিলেক্টর → `shops.business_type`-এ সেভ; **এখন শুধু সেলুন ফ্লো সম্পূর্ণ** (সিদ্ধান্ত ১০), পার্লার বাছলে সেলুনের ডিফল্টেই চলবে
- [x] **মোবাইল নম্বর বাধ্যতামূলক ফিল্ড** রেজিস্ট্রেশনে — BD ফরম্যাট ভ্যালিডেশনসহ (ভেরিফাই করা হয় না)
- [x] **ইমেইলে ভেরিফিকেশন কোড:** সাইনআপের পর কোড-এন্ট্রি স্ক্রিন, রিসেন্ড টাইমার, রেট-লিমিট, ভেরিফাই না হলে গেট
- [x] **প্রতিবার লগইনে কোড নেই** — শুধু সাইনআপে একবার
- [x] মাইগ্রেশন: `profiles.phone` NOT NULL কনস্ট্রেইন্ট/ব্যাকফিল, ইমেইল-ভেরিফিকেশন স্টেট (Supabase Auth-এর নিজস্ব ফিল্ড ব্যবহার করা যায় কিনা আগে যাচাই করো — নতুন কলামের দরকার না-ও হতে পারে)

**Acceptance:** নতুন দোকানদার সাইনআপে ধরন বাছে → ফোন দেয় → ইমেইল কোড ভেরিফাই করে → ড্যাশবোর্ডে পৌঁছায়; ভেরিফাই না করে অ্যাপে ঢোকা যায় না; পুরনো অ্যাকাউন্ট ভাঙে না।

  - (নোট: **কোনো নতুন migration লাগেনি।** `profiles.phone` DB-লেভেলে nullable-ই থাকছে — সাইনআপের প্রোফাইল-রো তৈরি করা DB ট্রিগারের SQL কোনো migration-এ কখনো capture হয়নি (শুধু লাইভ Supabase-এ আছে), তাই সেটার INSERT স্টেটমেন্ট phone কলাম নিয়ে কী করে না জেনে blind NOT NULL বসানো ঝুঁকিপূর্ণ ছিল — **ইউজারের সাথে কনফার্ম করে** এই স্প্রিন্টে শুধু app-level (zod) required রাখা হয়েছে, DB constraint বাদ। **ইমেইল-ভেরিফিকেশন স্টেটের জন্যও নতুন কলাম লাগেনি** — Supabase-এর "Confirm email" চালু থাকলে session তখনই ইস্যু হয় যখন কনফার্ম হয়, তাই "session আছে = ভেরিফায়েড" নিজেই সোর্স অব ট্রুথ, middleware-এ নতুন গেট লাগেনি।
  - **OTP মেকানিজম:** Supabase Auth-এর নিজস্ব signup-confirmation ইমেইলের `{{ .Token }}` (৬-ডিজিট) রিইউজ করা হয়েছে — কোনো কাস্টম কোড-টেবিল/ইমেইল-সার্ভিস/নতুন npm প্যাকেজ লাগেনি। `supabase.auth.verifyOtp({email, token, type:"signup"})` দিয়ে ভেরিফাই, `supabase.auth.resend({type:"signup", email})` দিয়ে রিসেন্ড (Supabase নিজেই rate-limit এনফোর্স করে, `translateAuthError`-এ আগে থেকেই হ্যান্ডলড)। ৬-বক্স OTP ইনপুট (`src/components/ui/OtpInput.tsx`) হ্যান্ড-রোল্ড (auto-advance/backspace/paste-split), কোনো নতুন ডিপেন্ডেন্সি ছাড়া।
  - **হ্যান্ড-অফ প্রয়োজন (ব্লকিং — কোড ফিচার সচল করতে এই দুটো Supabase Dashboard-এ ম্যানুয়ালি করতে হবে, কোনো Supabase MCP access নেই — [[project_supabase_mcp_access_gap]]):**
    1. **Authentication → Providers → Email → "Confirm email" চালু করো।** এখন এটা **বন্ধ** আছে (লাইভ QA-তে ধরা পড়েছে — নতুন সাইনআপে সাথে সাথে session ফেরত আসে, কোনো ভেরিফিকেশন লাগে না) — চালু না করলে কোড-স্ক্রিন কখনো দেখা যাবে না, `/complete-profile`-এ সরাসরি চলে যাবে (কোড এখন এই fallback path-টাই এক্সারসাইজ করে, গ্রেসফুল)।
    2. **Authentication → Email Templates → "Confirm signup"-এ বডি বদলে `{{ .Token }}` দেখাও** (এখন শুধু `{{ .ConfirmationURL }}` লিংক আছে ধরে নেওয়া হচ্ছে) — নাহলে ৬-ডিজিট কোড কখনো ইমেইলে পৌঁছাবে না। এই চ্যাটে exact টেমপ্লেট টেক্সট দেওয়া হয়েছে (bn কপি সহ)।
  - **`profiles.phone` লেখার আসল জায়গা:** OTP-ভেরিফাই সফল হলে (`verifyEmailCode`) *এবং* সাইনআপেই session ফেরত এলে (`signUp`, "Confirm email" বন্ধ থাকা অবস্থায় — এখনকার লাইভ অবস্থা) — দুই জায়গাতেই একই শেয়ার্ড হেল্পার (`applyPendingPhone`, `auth.api.ts`) `user_metadata.phone`-কে authenticated `.from("profiles").update()` কল দিয়ে `profiles.phone`-এ লেখে (ঠিক `completeOnboarding()`-এর প্যাটার্নে)। **লাইভ Playwright QA দিয়ে দুটোই ভেরিফায়েড:** নতুন কাস্টমার সাইনআপ করে `/account` পেজে গিয়ে ফোন নম্বর দেখা গেছে ("Confirm email" বন্ধ থাকার পাথ)। **কোড-ভেরিফাই পাথটা এই প্রজেক্টে লাইভ টেস্ট করা যায়নি** (real inbox লাগে + "Confirm email" বন্ধ) — কোড রিভিউ দিয়ে নিশ্চিত করা হয়েছে যে একই হেল্পার রিইউজ হচ্ছে, হ্যান্ড-অফ ২টা সম্পন্ন হওয়ার পর ভেরিফাই করা দরকার।
  - **`shops.business_type` প্রিফিল:** signup-এ বাছা ধরন `user_metadata.business_type`-এ থাকে (shops টেবিল তো signup-এর সময় থাকেই না), প্রথম শপ-সেভে (`ShopSettingsForm.tsx`, shop===null) সেটা পড়ে চিপ prefill করে — **লাইভ QA-তে ভেরিফায়েড** (PARLOUR বেছে সাইনআপ করে সেটিংস পেজে গিয়ে PARLOUR চিপ প্রি-সিলেক্টেড পাওয়া গেছে)। `ShopSettingsForm.tsx`-এ শুধু এই একটা ফলব্যাক-লাইন বদলেছে, Sprint 19-এর কাজ (is_open ডুপ্লিকেট পিল সরানো) অক্ষত।
  - **`/complete-profile` থেকে phone সরানো হয়েছে** (এখন register-এই বাধ্যতামূলক, দুইবার জিজ্ঞেস করা এড়াতে) — gender/avatar/skip অক্ষত। `onboardingDict.pageSubtitle`-ও আপডেট (আর "ফোন" উল্লেখ নেই)।
  - **`ChipGroup` শেয়ার্ড কম্পোনেন্ট** (`src/components/ui/ChipGroup.tsx`) নতুন — register ফর্মের role/business-type চিপ দুটোই এটা দিয়ে। বিদ্যমান কপি-পেস্টেড চিপ প্যাটার্ন (`WalkInDialog.tsx`, `ShopSettingsForm.tsx`) migrate করা হয়নি — এই স্প্রিন্টের স্কোপ না, ঠিক Sprint 12-এর `BottomSheet`-এর মতোই ইচ্ছাকৃত।
  - **`LoginForm.tsx`-এ ছোট সংযোজন:** `email_not_confirmed` এরর এলে এখন "কোড ভেরিফাই করো" লিংক দেখায় (`/verify-email?email=...`)। এই প্রজেক্টের বর্তমান কনফিগে (Confirm email বন্ধ) এই এরর-পাথ রিপ্রোডিউস করা যায়নি — কোড রিভিউ দিয়ে যাচাই, Confirm email চালু হওয়ার পর লাইভ ভেরিফাই করা দরকার।
  - **যাচাই:** `npm run build`/`lint`/`test` তিনটাই গ্রিন (একই প্রি-এক্সিস্টিং বেসলাইন — ২টা lint error `design/design_handoff_palaa/`-এর, ১৩টা warning পুরনো react-hook-form প্যাটার্ন)। Playwright দিয়ে: ৩২০px+ডেস্কটপ register (কাস্টমার/প্রোভাইডার দুটোই), ব্যবসার-ধরন-ছাড়া সাবমিট ব্লকড (ভ্যালিডেশন এরর দেখা গেছে), phone persist থেকে account পেজ পর্যন্ত, business_type prefill শপ-সেটিংসে, verify-email পেজ (দুই ভিউপোর্ট, OTP অটো-অ্যাডভান্স, রিসেন্ড কুলডাউন) — একটা রিয়েল ৩২০px ওভারফ্লো বাগ পাওয়া গেছে ও ফিক্স হয়েছে (নিচে দেখো), পুরনো টেস্ট অ্যাকাউন্ট দিয়ে লগইন ভাঙেনি (কোনো কোড চাওয়া হয়নি, বিদ্যমান সেশন-ফ্লো অক্ষত)।
  - **রিয়েল বাগ পাওয়া গেছে ও ফিক্স:** `OtpInput`-এর ৬টা বক্স (৪৪px প্রতিটা) + `gap-2` (৮px) মিলে ৩০৪px হয়ে যাচ্ছিল, কিন্তু ৩২০px ভিউপোর্টে AuthShell-এর প্যাডিং বাদ দিয়ে মাত্র ২৮৮px জায়গা ছিল — ১৬px হরাইজন্টাল ওভারফ্লো (Playwright-এর `scrollWidth`/`clientWidth` চেকে ধরা পড়ে)। ফিক্স: বেস গ্যাপ `gap-2` → `gap-1` (৪px), `sm:`-এ আগের মতোই `gap-2` — এখন ৩২০px-এ ঠিক ফিট করে (২৮৪px ব্যবহৃত)।

### Sprint 14 — লাইভ সিরিয়াল বোর্ড ২.০ ⭐

**লক্ষ্য:** অ্যাপের সবচেয়ে বেশি ব্যবহৃত স্ক্রিনটার পুরো নতুন লুক — স্টাফ-কেন্দ্রিক, মোবাইলে সত্যিকারের ব্যবহারযোগ্য, আর কাজ শেষে পেমেন্ট নেওয়ার আসল ফ্লো।

- [x] **স্টাফ কার্ড:** প্রতিটা স্টাফের প্রোফাইল পিক + নাম + ব্যাকলগ + অপেক্ষমাণ সংখ্যা; সেই কার্ডের **ভেতরেই** তার কাস্টমার সিরিয়ালগুলো (`chairs.staff_avatar_url` ইতিমধ্যেই আছে)
- [x] **মোবাইলে স্টাফ কার্ড একটার নিচে আরেকটা** — এখনকার হরাইজন্টাল স্ক্রল বাদ (`QueueBoard.tsx`-এর `flex overflow-x-auto`); ডেস্কটপে কলাম গ্রিড থাকবে
- [x] **ওয়াক-ইন (অফলাইন কাস্টমার) ডায়ালগ রিডিজাইন:** কাস্টমারের নাম + সার্ভিস লিস্ট + **ছবিসহ ছোট স্টাফ কার্ডের পিকার** (এখন শুধু টেক্সট চিপ); মোবাইলে বটম শিট
- [x] **রানিং (Now Serving) কার্ড:**
  - মেসেজ আইকন **বাদ** — কাজ চলাকালীন মেসেজের দরকার নেই
  - **ম্যানুয়াল টাইম বাড়ানোর অপশন** (+৫ / +১০ / কাস্টম মিনিট) — এস্টিমেটের চেয়ে বেশি সময় লাগতে পারে, তখন ETA রি-ক্যালকুলেট হবে
  - "বাকি রেখে সম্পন্ন করো" টেক্সট লিংক **বাদ** — পেমেন্ট শিটই এটা কন্ট্রোল করবে
- [x] **অপেক্ষমাণ সিরিয়াল কার্ড:** কাস্টমারের **ছবি/অ্যাভাটার** (দেখে চেনার জন্য — `profiles.avatar_url` join করতে হবে) · **মেসেজ অপশন এখানে থাকবে** · **Move বাটন ঠিকমতো রেন্ডার** (কাজ করে কিন্তু প্রপারলি শো করে না)
- [x] **পেমেন্ট কনফার্মেশন শিট:** "✓ কাজ সম্পন্ন" → শিট খুলবে → **ক্যাশ / মোবাইল ব্যাংকিং / বাকি** (শপ সেটিংসে যেগুলো চালু, শুধু সেগুলো) → কনফার্ম → সেই অনুযায়ী ইনকামে বা বাকির খাতায় যাবে
- [x] মাইগ্রেশন: `serials.payment_method`, `serials.extended_min`

**Acceptance:** ৩২০px ফোনে পুরো বোর্ড ব্যবহারযোগ্য (স্টাফ স্ট্যাকড, সব বাটন ট্যাপযোগ্য); পেমেন্ট শিটে ক্যাশ বাছলে ইনকামে যোগ হয়, বাকি বাছলে বাকির খাতায় যায়; টাইম বাড়ালে পরের সবার ETA পিছায়; Move মেনু সঠিকভাবে দেখা যায়।

  - (নোট: **প্রথম প্রম্পটে** শুধু প্রথম ৩টা চেকবক্স করা হয়েছিল (স্টাফ কার্ড, মোবাইল স্ট্যাকিং, ওয়াক-ইন রিডিজাইন — কমিট `f3f16d4`)। **দ্বিতীয় প্রম্পটে** বাকি ৩টা (Now Serving কার্ড, অপেক্ষমাণ কার্ডের অ্যাভাটার+Move ফিক্স, পেমেন্ট কনফার্মেশন শিট) + মাইগ্রেশন সম্পূর্ণ হয়েছে — স্প্রিন্ট এখন সম্পূর্ণ। নিচের নোটগুলো দ্বিতীয় প্রম্পটের কাজ নিয়ে।
  - **`ChairColumn.tsx`** এখন একটা প্রকৃত "স্টাফ কার্ড" (`rounded-2xl border border-line bg-soft/60 p-3` আউটার শেল) — ভেতরের `NowServingCard`/`WaitingRow`-এর নিজস্ব কার্ড-স্টাইল (rounded-[22px] bg-accent / rounded-2xl bg-card) **অপরিবর্তিত রাখা হয়েছে**, তাই ডাবল-বর্ডার/কনফ্লিক্ট এড়াতে আউটার শেল ইচ্ছাকৃতভাবে নরম/নিউট্রাল। রঙিন ডট সরিয়ে `AvatarChip` (Sprint 12, `staff_avatar_url` + label fallback) বসানো হয়েছে হেডারে।
  - **`QueueBoard.tsx`** lane container-এর বেসলাইন এখন `flex flex-col` (ভার্টিকাল স্ট্যাক), `overflow-x-auto` সম্পূর্ণ সরানো হয়েছে — `md:`+ এ আগের কলাম-গ্রিড অক্ষত (Sprint 12-এর ব্রেকপয়েন্ট কনভেনশন মেনে ডেস্কটপ এখন এনহ্যান্সমেন্ট, বেসলাইন না)।
  - **`WalkInDialog.tsx`** এখন `BottomSheet` (Sprint 12) দিয়ে — মোবাইলে বটম-শিট (ড্র্যাগ-হ্যান্ডল সহ), ডেস্কটপে সেন্টারড ডায়ালগ, backdrop-click/Escape ক্লোজ ফ্রি পেয়ে গেছে। সার্ভিস-চিপ অপরিবর্তিত (আগে থেকেই ঠিক প্যাটার্নে); চেয়ার/স্টাফ পিকার এখন `AvatarChip`+নাম+backlog দেখানো ছোট কার্ড (আগে প্লেইন টেক্সট চিপ ছিল), "অটো" অপশন Zap-আইকন সার্কেলে। `ChipGroup` (Sprint 13) এক্সটেন্ড করা হয়নি — ওটা টেক্সট/আইকন-অনলি, ফটো-ভ্যারিয়েন্টের প্রয়োজন এই একটা জায়গাতেই, তাই ইনলাইন রাখা হয়েছে (over-abstraction এড়াতে, Sprint 12-এর precedent অনুযায়ী)।
  - **লাইভ Playwright QA (provider টেস্ট অ্যাকাউন্ট, `Palaa Test Salon`):** ৩২০px + ডেস্কটপ দুই ভিউপোর্টে `/dashboard` স্ক্রিনশট — কোনো হরাইজন্টাল ওভারফ্লো নেই (`scrollWidth`/`clientWidth` চেক ক্লিন) দুই স্ট্যাফ কার্ডেই (Rahim ~15 মিন ব্যাকলগ+চলমান সিরিয়াল, Karim ফাঁকা)। ওয়াক-ইন ডায়ালগ মোবাইলে বটম-শিট + ডেস্কটপে ডায়ালগ দুটোই স্ক্রিনশটে কনফার্মড, চেয়ার-পিকারে ছবি/initial+নাম+backlog ঠিকমতো দেখা গেছে। একটা real walk-in সাবমিট করে পুরো ফ্লো এন্ড-টু-এন্ড ভেরিফাই করা হয়েছে (auto-chair অ্যাসাইন সঠিক চেয়ারে গেছে, count আপডেট হয়েছে) — টেস্ট ডেটা পরে no-show দিয়ে ক্লিনআপ করা হয়েছে (এই টেস্ট অ্যাকাউন্ট real Supabase প্রজেক্টে, লোকাল DB না)।
  - **যাচাই:** `npm run build`/`lint`/`test` তিনটাই গ্রিন, একই প্রি-এক্সিস্টিং বেসলাইন (২টা lint error `design/design_handoff_palaa/`-এর, ১৩টা warning পুরনো react-hook-form প্যাটার্ন — নতুন কিছু যোগ হয়নি)।

  - **হ্যান্ড-অফ প্রয়োজন (ব্লকিং):** `supabase/migrations/20260808_serial_time_extension_and_payment_method.sql` এখনো লাইভ প্রজেক্টে রান হয়নি (কোনো Supabase MCP access নেই — [[project_supabase_mcp_access_gap]])। রান না হওয়া পর্যন্ত: টাইম-এক্সটেনশন বাটন (+৫/+১০/কাস্টম) আর পেমেন্ট শিটের "নিশ্চিত করো" — দুটোই DB এরর দেবে (`estimated_duration_min`/`extended_min`/`payment_method`/`customer_avatar_url` কলাম এখনো নেই), যদিও UI নিজে ঠিকমতো রেন্ডার করে (লাইভ QA-তে ভেরিফায়েড, নিচে দেখো)। মাইগ্রেশন রান হওয়ার পর এই দুটো মিউটেশন লাইভ ভেরিফাই করা দরকার।
    - **⚠️ Sprint 17-এ ধরা পড়েছে — এই ফাইলটাই আসলে খালি কমিট হয়ে গিয়েছিল** (git history: ১ বাইট, শুধু স্পেস), অর্থাৎ রান করার মতো কোনো SQL কখনোই ছিল না। Sprint 17 শুরুর আগে ফাইলটা পুনর্গঠন করে ঠিক করা হয়েছে (Sprint 17-এর নোট দেখো) — তাই এই হ্যান্ড-অফ এখনো ব্লকিং, কিন্তু এখন ফাইলে আসল SQL আছে।
  - **কাস্টমার অ্যাভাটার — কেন RLS না খুলে snapshot বেছে নেওয়া হলো:** `profiles`-এর RLS ইচ্ছাকৃতভাবে `auth.uid() = id`-এই সীমাবদ্ধ (booking-এর সময় `customer_name`/`customer_phone` snapshot করার পুরনো কারণটাই — cross-user profile read এড়ানো)। নতুন RLS পলিসি খুলে দেওয়ার বদলে, একই booking-time snapshot প্যাটার্নে `avatar_url`-ও `serials.customer_avatar_url`-এ snapshot করা হয়েছে (`serial_before_insert()` ট্রিগারে একলাইন সংযোজন, migration-এ পূর্ণ ফাংশন-বডি reproduced)। ট্রেড-অফ: প্রোফাইল-ছবি পরে বদলালে বোর্ডে আপডেট হবে না — customer_name/phone-এও আগে থেকে এই একই ট্রেড-অফ মানা আছে।
  - **টাইম-এক্সটেনশন কেন নতুন RPC না লেগে প্লেইন `.update()`:** `recalc_queue_estimates` আগে শুধু status/chair_id বদলালে ফায়ার হতো (`serial_after_update` ট্রিগার) — `estimated_duration_min` বদলালে ETA স্টেল থেকে যেত। migration-এ ট্রিগারে একটা নতুন `if new.estimated_duration_min is distinct from old.estimated_duration_min` ব্লক যোগ হয়েছে (পুরনো ফাংশন-বডি অক্ষত, শুধু এই ব্লক নতুন) — এখন প্লেইন client `.update()`-ই যথেষ্ট, বাকি সব মিউটেশনের মতো একই "thin client, smart DB" প্যাটার্নে।
  - **পেমেন্ট মেথড enabled/disabled সোর্স:** নতুন `PAYMENT_METHOD_VALUES`/`ENABLED_PAYMENT_METHODS` (`src/config/constants.ts`) — এখন শুধু `["cash"]` enabled, বাকি "শীঘ্রই আসছে"। `/payment-methods` পেজ (`PaymentMethodsView.tsx`) আর নতুন `PaymentConfirmSheet.tsx` দুটোই এই এক সোর্স থেকে পড়ে (আগে `PaymentMethodsView.tsx`-এ hardcoded ছিল) — `provider-queue`/`provider-payment-methods` দুই আলাদা ফিচার হওয়ায় (`eslint-plugin-boundaries` রুল অনুযায়ী একে অপরকে import করতে পারে না) shared কনস্ট্যান্ট `src/config`-এ রাখা হয়েছে। **Sprint 18** যখন সত্যিকারের per-shop persistence আনবে, শুধু এই একটা জায়গার সোর্স dynamic করলেই দুই জায়গায় কাজ করবে।
  - **"বাকি" পেমেন্ট-মেথড না** — due-ledger-এ যাওয়ার তৃতীয়, সবসময়-উপলব্ধ অপশন, `ENABLED_PAYMENT_METHODS` ফিল্টারের বাইরে। `completeSerial()`-এর সিগনেচার বদলেছে (`due?: {amount}` → `payment: {method} | {due}`) — cash/মোবাইল-ব্যাংকিং বাছলে `payment_method` সেট হয়ে `payment_status='PAID'`, বাকি বাছলে আগের মতোই `payment_status='DUE'` কিন্তু `payment_method: null` (কিছু আসলে কালেক্ট হয়নি বলে)। `payment_method`-এর ভ্যালু lowercase কনভেনশন Sprint 9-এর `advance_method`-এর সাথে সামঞ্জস্যপূর্ণ।
  - **Move মেনু বাগ ইতিমধ্যেই ফিক্স ছিল** — কোড রিভিউ করে কোনো z-index/positioning সমস্যা পাওয়া যায়নি; সম্ভবত `ChairColumn`-এর পুরনো `overflow-x-auto` (এই স্প্রিন্টেরই প্রথম প্রম্পটে সরানো) দায়ী ছিল। লাইভ Playwright QA-তে ডেস্কটপে মেনু পরিষ্কারভাবে খুলতে দেখা গেছে, কোনো ক্লিপিং নেই। একইসাথে `MoveSerialMenu.tsx`-এর হার্ডকোডেড ইংরেজি টেক্সট ("Move"/"Moving…"/"No eligible chair...") `providerQueueDict`-এ এনে bilingual করা হয়েছে (আগে থেকে ছিল bug, ফাইলটা এমনিতেই খোলা ছিল বলে ঠিক করে দেওয়া হলো)।
  - **লাইভ Playwright QA:** পুরনো seed serial-এর বদলে একটা থ্রো-অ্যাওয়ে ওয়াক-ইন বানিয়ে স্টার্ট করে NowServingCard পরীক্ষা করা হয়েছে (মেসেজ আইকন নেই, এক্সটেন্ড-পিল দেখা যাচ্ছে, পেমেন্ট শিট Cash+বাকি দেখাচ্ছে) — শেষে migration-নির্ভর "সম্পন্ন" এর বদলে **cancel** দিয়ে ক্লিনআপ করা হয়েছে (cancel নতুন কলাম ছোঁয় না, তাই migration ছাড়াও নিরাপদ)। WaitingRow-এ কাস্টমার অ্যাভাটার+পজিশন-ব্যাজ ঠিকমতো দেখা গেছে (নতুন কলাম না থাকায় initial-fallback, এটাই এক্সপেক্টেড)। ৩২০px+ডেস্কটপ দুই ভিউপোর্টেই ওভারফ্লো-মুক্ত।
  - **এনভায়রনমেন্ট নোট:** এই সেশনে `TaskStop`-এর পরও পুরনো `next dev` প্রসেস পোর্ট ৩০০০-এ চলতে থেকেছে (২ বার) — ম্যানুয়াল `taskkill` দিয়ে ঠিক করা হয়েছে, memory-তে নোট করা আছে ([[project_agent_environment_quirks]])।

### Sprint 15 — সার্ভিস ক্যাটালগ + স্টাফ ম্যাট্রিক্স + অফার

- [x] **ইমোজি → রিয়েল ছবি (দোকানদার আপলোড):** ইউজার নিজে এই সিদ্ধান্ত পাল্টেছেন — মূল পরিকল্পনা ছিল শুধু ভেক্টর আইকন, কিন্তু প্রম্পটেই বলা হয় "রিয়াল ইমেজ দিবো (চুল কাটা হলে তার পিক)"। ফলাফল: `services.image_url` (নতুন কলাম) + `ImageUploadField` দিয়ে আপলোড; ছবি না থাকলে `services.category`-ভিত্তিক lucide আইকন ফলব্যাক (`SERVICE_CATEGORY_ICON`, আগে থেকেই ছিল, শুধু ব্যবহার হচ্ছিল না)। নাম-অনুমান করা `serviceEmoji()`/`service-icon.ts` রিটায়ার (ফাইল ডিলিট)।
- [x] **সার্ভিস স্ট্যাটাস স্পষ্ট হাইলাইট** — Sprint 12-এর `StatusPill` দিয়ে (আগের ছোট "● word" টেক্সট বদলানো হয়েছে)
- [x] **প্রতি সার্ভিস কার্ডে এডিট + ডিলিট** — ডিলিটে `ConfirmSheet`; অ্যাক্টিভ সিরিয়ালে ব্যবহৃত হলে সতর্কবার্তা
- [x] **"কে কোন সার্ভিস করতে পারে" ম্যাট্রিক্স:** টেবিল হেডারে স্টাফের ছবি (`AvatarChip`)
- [x] **ম্যাট্রিক্স মোবাইলে:** স্টাফ-প্রতি অ্যাকর্ডিয়ন কার্ড
- [x] **অফার সম্পূর্ণ CRUD:** আপডেট/এডিট/ডিলিট যোগ হয়েছে
- [x] মাইগ্রেশন: `services.image_url`

**Acceptance:** কোথাও ইমোজি আইকন নেই ✅; মোবাইলে ম্যাট্রিক্স স্ক্রল ছাড়া ব্যবহারযোগ্য ✅ (320px Playwright-এ ওভারফ্লো-মুক্ত কনফার্মড); অফার এডিট ও ডিলিট দুটোই কাজ করে ✅।

  - **ছবির সোর্স — সিদ্ধান্ত পাল্টেছে প্রম্পটেই:** পরিকল্পনায় ছিল শুধু ক্যাটাগরি-ভিত্তিক ভেক্টর আইকন। ইমপ্লিমেন্টেশন শুরুর পরই তুমি বলো রিয়েল ছবি চাও (যেমন চুল-কাটার আসল ছবি)। `AskUserQuestion` দিয়ে জিজ্ঞেস করা হয় ছবি কোথা থেকে আসবে — উত্তর: "দোকানদার নিজে আপলোড করবে"। তাই বিদ্যমান `ImageUploadField`/`uploadShopImage()` (shop logo/cover/staff-avatar-এ আগে থেকেই ব্যবহৃত `shop-media` বাকেট) রিইউজ করা হয়েছে, `UploadKind`-এ নতুন `"service"` যোগ করে — কোনো নতুন storage bucket/RLS লাগেনি। ছবি না থাকলে (বেশিরভাগ পুরনো সার্ভিসেই থাকবে না, migration-এর পরও) category আইকন ফলব্যাক দেখায় — তাই মূল "ইমোজি সরাও" অ্যাক্সেপট্যান্স-ও পূরণ হয় সবসময়, ছবি আপলোড না করলেও।
  - **গুরুত্বপূর্ণ সেফটি ফিক্স — নিজে ধরা একটা ডিজাইন বাগ:** প্রথম ড্রাফটে `image_url` zod schema-তে `.default(null)` ছিল আর ফর্মের `defaultValues`-এও `?? null` — মানে **প্রতিটা** সার্ভিস তৈরি/এডিটেই (এমনকি ছবি না ছুঁলেও) পেলোডে `image_url: null` পাঠানো হতো। যেহেতু মাইগ্রেশন এখনো লাইভ প্রজেক্টে রান হয়নি, এটা করলে **পুরো সার্ভিস অ্যাড/এডিট ফিচারটাই ভেঙে যেত** — শুধু নতুন ছবি-ফিচার না, বেসিক ক্যাটালগ ম্যানেজমেন্টও। ফিক্স: `.default(null)` সরিয়ে `image_url` optional-ই রাখা হয়েছে (কোনো ডিফল্ট না), আর ফর্মের ডিফল্ট ভ্যালু `initial?.image_url ?? undefined` (না যে `null`) — তাই না-ছোঁয়া থাকলে ভ্যালুটা `undefined` থাকে, আর `JSON.stringify` (Supabase ক্লায়েন্ট যেভাবে রিকোয়েস্ট বডি বানায়) `undefined` কী বাদ দিয়ে দেয়, ফলে কলাম-না-থাকা ডাটাবেসের বিরুদ্ধেও পুরোনো সব ফ্লো অক্ষত থাকে। মাইগ্রেশন রান হওয়ার পরই শুধু ছবি-আপলোড ফিচারটা কাজ করবে।
  - **সার্ভিস ডিলিট — হার্ড ডিলিট বনাম আর্কাইভ, কীভাবে বাছা হলো:** কোড রিভিউ করে দেখা গেছে `services` টেবিলে RLS-এ আগে থেকেই `"services: owner delete"` পলিসি আছে (`capture_baseline_queue_engine.sql`) — মানে schema ডিজাইনে হার্ড ডিলিট এমনিতেই ইচ্ছাকৃত ছিল, শুধু UI বাটন কখনো বানানো হয়নি। কিন্তু `chair_service_stats.service_id`-এর FK constraint কোনো ট্র্যাক করা migration-এ নেই (base টেবিল migration history-র আগের) — তাই ON DELETE বিহেভিয়ার অজানা। সমাধান: **নতুন `deleteService()` আগে সত্যিকারের `DELETE` চেষ্টা করে**; Postgres যদি foreign-key violation (`23503`) দেয়, স্বয়ংক্রিয়ভাবে `setServiceActive(id, false)`-এ fallback করে (টোস্ট: "সরাসরি মুছে ফেলা যায়নি, তাই বন্ধ করে দেওয়া হয়েছে")। এছাড়াও, ডিলিটের আগে `isServiceInActiveUse()` চেক করে সার্ভিসটা এখন কোনো WAITING/IN_PROGRESS সিরিয়ালে আছে কিনা (`serials.service_ids` array-তে `.contains()`) — থাকলে `ConfirmSheet`-এ আলাদা সতর্কবার্তা দেখায় (সেই কাস্টমারের কাজ প্রভাবিত হবে না যেহেতু `services_snapshot` ফ্রোজেন, শুধু নতুন বুকিংয়ে আর দেখা যাবে না)।
  - **অফার ডিলিট সহজ (কোনো guard লাগেনি):** `offers`-এর `service`-এর মতো কোনো downstream রেফারেন্স (serials/chair_service_stats) নেই, RLS-ও আগে থেকে `for all` (delete সহ) — তাই সরাসরি হার্ড ডিলিট, কোনো active-use চেক ছাড়াই।
  - **অফার এডিট:** `OfferForm.tsx`-এ `initial?: Offer` prop যোগ (ServiceForm-এর প্যাটার্ন মিরর করে), `valid_until` ISO টাইমস্ট্যাম্প থেকে date-input-এর জন্য লোকাল yyyy-mm-dd-এ কনভার্ট করা হয়েছে (UTC কনভার্সনে টাইমজোন-শিফট এড়াতে `toISOString()` না ব্যবহার করে লোকাল `getFullYear/getMonth/getDate`)।
  - **ম্যাট্রিক্স মোবাইল ডিজাইন:** সিঙ্গল-ওপেন অ্যাকর্ডিয়ন (একটা স্টাফ কার্ড খুললে আগেরটা বন্ধ) — প্রতি কার্ডের হেডারে ছবি+নাম+"X/Y সার্ভিস পারে" কাউন্ট, খুললে টগল-লিস্ট। ডেস্কটপ টেবিল ও মোবাইল অ্যাকর্ডিয়ন দুটোই একই `canPerform()`/mutation লজিক শেয়ার করে, শুধু রেন্ডারিং আলাদা (`hidden md:block` / `md:hidden`)।
  - **QA পরিবেশগত নোট:** এই সেশনে আগে ভুলবশত ইউজারের আসল dev server কিল হয়ে গিয়েছিল (দেখুন [[project_agent_environment_quirks]])। তাই এবার port 3000-এ কিছু টাচ না করে **আলাদা git worktree** (`../queueflow-qa`, node_modules `robocopy /E` দিয়ে কপি — junction/symlink করলে Turbopack "points out of filesystem root" এরর দেয়) বানিয়ে সম্পূর্ণ আইসোলেটেড dev server-এ (পোর্ট 3001) QA করা হয়েছে, শেষে worktree + প্রসেস ক্লিনআপ করে ইউজারের আসল সার্ভার অক্ষত রাখা হয়েছে।
  - **হ্যান্ড-অফ প্রয়োজন (ব্লকিং, শুধু ছবি-ফিচারের জন্য):** `supabase/migrations/20260815_service_photo.sql` এখনো লাইভ প্রজেক্টে রান হয়নি। রান না হওয়া পর্যন্ত সার্ভিস অ্যাড/এডিট/ডিলিট, ক্যাটাগরি-আইকন ফলব্যাক, ম্যাট্রিক্স, অফার CRUD — সব স্বাভাবিক কাজ করবে (কোনো নতুন কলামের উপর নির্ভর করে না); শুধু "সার্ভিসের ছবি" আপলোড করে সেভ করলে তখনই DB এরর দেবে।
  - **যাচাই:** `npm run build`/`lint`/`test` তিনটাই গ্রিন (একই প্রি-এক্সিস্টিং বেসলাইন — ২টা lint error `design/design_handoff_palaa/`-এর, নতুন কিছু যোগ হয়নি)। লাইভ Playwright QA (provider টেস্ট অ্যাকাউন্ট): ডেস্কটপে থ্রো-অ্যাওয়ে সার্ভিস তৈরি→ক্যাটাগরি-আইকন দেখা গেছে→হার্ড ডিলিট কনফার্মড কাজ করছে; অফার তৈরি→এডিট→ডিলিট তিনটাই কনফার্মড কাজ করছে (প্রথম রান-এ টেস্ট-স্ক্রিপ্ট টাইমিং ফ্লেকিনেসে "ডিলিট ব্যর্থ" ভুল রিপোর্ট এসেছিল, আলাদা debug রান দিয়ে কনফার্ম করা হয়েছে ফিচারটা আসলে ঠিক কাজ করে)। ৩২০px+ডেস্কটপ দুই ভিউপোর্টেই সার্ভিস/অফার/ম্যাট্রিক্স স্ক্রিনশট নেওয়া হয়েছে, কোনো হরাইজন্টাল ওভারফ্লো নেই। মোবাইল অ্যাকর্ডিয়ন খুলে-বন্ধ করে টগল-লিস্ট ঠিকমতো কাজ করা কনফার্মড।

### Sprint 16 — মেসেজ ২.০ (চ্যাট রিডিজাইন) — **দুই সাইড একসাথে**

**লক্ষ্য:** "চ্যাট" → **"মেসেজ"**, এবং আধুনিক মেসেজিং অ্যাপের চেনা UI প্যাটার্নে রিডিজাইন (রেফারেন্স: Chatterly-ধরনের লিস্ট+থ্রেড লেআউট)।

> **কেন এই স্প্রিন্টটা দুই সাইডের:** `ChatThreadList` ও `ChatThreadView` কম্পোনেন্ট দোকানদার ও
> কাস্টমার দুই সাইডেই শেয়ার্ড। একবার রিডিজাইন করলে দুই জায়গাতেই পড়ে — আলাদা করলে ডুপ্লিকেট কাজ হতো।

- [x] **নামকরণ:** সব জায়গায় "চ্যাট" → "মেসেজ" — **দুই সাইডেই** (প্রোভাইডার সাইডবার, কাস্টমার সাইড প্যানেল, বটম ন্যাভবার, হেডার, টাইটেল, এম্পটি স্টেট — bn ও en দুটোতেই)
- [x] **লিস্ট রিডিজাইন:** বড় গোল অ্যাভাটার · নাম + শেষ মেসেজের প্রিভিউ · সময় · unread কাউন্ট ব্যাজ · উপরে সার্চ বার · ডেলিভারি টিক
- [x] **থ্রেড রিডিজাইন:** হেডারে অ্যাভাটার + last seen · নতুন বাবল স্টাইল (নিজেরটা রঙিন/ডানে, অন্যেরটা সাদা/বামে) · ইমেজ অ্যাটাচমেন্ট গ্রিড · নিচে পিল-শেপ ইনপুট বার (অ্যাটাচ / ক্যামেরা / সেন্ড)
- [x] **কল/ভিডিও আইকন নেই** (সিদ্ধান্ত ১৪) — শুধু মেসেজ (আগে থেকেই কোনো ছিল না, নতুন কিছু সরাতে হয়নি)
- [x] মোবাইলে ফুল-স্ক্রিন থ্রেড, কীবোর্ড উঠলে ইনপুট বার ঢাকা পড়বে না
- [x] ডেস্কটপে স্প্লিট-প্যানেল (তোমার প্রম্পটেই কনফার্মড সিদ্ধান্ত) + মাইগ্রেশন: `messages.image_urls` (একাধিক ছবি, তোমার প্রম্পটেই কনফার্মড সিদ্ধান্ত)

**Acceptance:** দুই সাইডেই "মেসেজ" — কোথাও "চ্যাট" নেই ✅; লিস্টে বড় গোল অ্যাভাটার+সার্চ+unread কাউন্ট+টিক ✅; থ্রেডে ছবি-গ্রিড+ক্যামেরা বাটন ✅; ডেস্কটপে স্প্লিট-প্যানেল, মোবাইলে ফুল-স্ক্রিন ✅ (Playwright QA-তে কনফার্মড)।

  - **পোস্ট-শিপ বাগ রিপোর্ট ("picture upload deya jacche nah") — দুইটা আলাদা ইস্যু পাওয়া গেছে ও ফিক্স করা হয়েছে:**
    1. আমার নিজের বাগ: `sendMessage()` সব ছবি-পাঠানোকেই (এমনকি ১টা ছবিও) নতুন `image_urls` কলামে রুট করছিল, যেটা মাইগ্রেশন ছাড়া লাইভ প্রজেক্টে নেই — ফলে যেকোনো ছবি পাঠানোই ভেঙে যাচ্ছিল, শুধু মাল্টি-সিলেক্ট না। ফিক্স (কমিট `783d01f`): ১টা ছবি হলে পুরনো `image_url` কলামে যায় (আগে থেকেই লাইভ, মাইগ্রেশন লাগে না), শুধু ২+ ছবি হলে নতুন `image_urls`-এ যায়।
    2. একটা **প্রি-এক্সিস্টিং, Sprint 16-এর সাথে সম্পর্কহীন** ইনফ্রাস্ট্রাকচার বাগ: আইসোলেটেড QA-তে সরাসরি টেস্ট করে দেখা গেছে কাস্টমার ছবি পাঠাতে পারে, কিন্তু **দোকানদার (শপ ওনার) কখনোই পারে না** — RLS এরর "new row violates row-level security policy" আসে, নিজের শপ হওয়া সত্ত্বেও। কারণ: `chat-media` স্টোরেজ বাকেটের আপলোড পলিসি (`20260803` মাইগ্রেশন) owner-চেকের জন্য একটা raw inline `EXISTS` সাবকোয়েরি ব্যবহার করে, যেখানে বাকি সব owner-চেক পলিসি (shops/services/chairs/serials) প্রমাণিত `is_shop_owner()` SECURITY DEFINER হেল্পার ব্যবহার করে। নতুন হ্যান্ড-অফ মাইগ্রেশন (`20260817_fix_chat_media_owner_upload_rls.sql`) পলিসিটা সেই একই হেল্পার ব্যবহার করতে বদলে দেয়। **এটাও ব্লকিং** — এই মাইগ্রেশন রান না হওয়া পর্যন্ত দোকানদার সাইড থেকে কোনো ছবি পাঠানো যাবে না (কাস্টমার সাইড থেকে এখনই কাজ করছে, ভেরিফায়েড)।
  - **দুই সিদ্ধান্ত প্রম্পটেই কনফার্মড:** (১) ডেস্কটপে সত্যিকারের স্প্লিট-প্যানেল (Chatterly/WhatsApp-Web স্টাইল, লিস্ট+থ্রেড পাশাপাশি) — আগে দুটো সম্পূর্ণ আলাদা ফুল-পেজ রাউট ছিল, রিস্টাইল-অনলি অপশনের বদলে এটাই বেছে নেওয়া হয়েছে। (২) একই মেসেজে একাধিক ছবি গ্রিড লেআউটে — নতুন মাইগ্রেশন লাগবে, রিস্টাইল-অনলি সিঙ্গল-ইমেজ অপশনের বদলে এটাই বেছে নেওয়া হয়েছে।
  - **রাউট-স্ট্রাকচার অসামঞ্জস্য, দুই সাইডে দুই কৌশল:** প্রোভাইডার সাইডে `/chat` আর `/chat/[customerId]` একই প্যারেন্ট ফোল্ডারে থাকায় নতুন `src/app/(provider)/chat/layout.tsx` (client component, `useParams()`) দিয়ে **প্রকৃত DOM-পার্সিস্টেন্ট লিস্ট** বানানো গেছে — থ্রেড খোলা/বন্ধ করলে লিস্ট কখনো রিমাউন্ট হয় না, তাই কোনো রিফেচ/ফ্লিকার নেই। কাস্টমার সাইডে `/chats` আর `/explore/[shopId]/chat`-এর কোনো কমন রাউট-সেগমেন্ট নেই (পুরো কাস্টমার শেল রিস্ট্রাকচার করা এই স্প্রিন্টের স্কোপের বাইরে) — তাই দুটো পেজ নিজে নিজে `ChatSplitShell` ব্যবহার করে, নেভিগেশনে লিস্ট রিমাউন্ট হয় কিন্তু react-query cache (`staleTime: 5min`) থেকে সাথে সাথেই রেন্ডার হয়, কোনো স্পিনার-ফ্লিকার নেই — লিস্ট-লেভেলে কোনো realtime subscription নেই যা হারানোর মতো। ইচ্ছাকৃত, ডকুমেন্টেড ট্রেড-অফ।
  - **লাইভ QA-তে ধরা একটা রিয়েল বাগ:** `CustomerShell.tsx`-এর `main` ট্যাগ সব কাস্টমার পেজেই `max-w-md` (448px) দিয়ে সীমাবদ্ধ (মোবাইল-ফার্স্ট রিডিং-উইথ ডিজাইন, বাকি সব পেজের জন্য ইচ্ছাকৃত) — এই কনস্ট্রেইন্টের ভেতরে ডেস্কটপ স্প্লিট-প্যানেল ভেঙে একদম চিপকে যাচ্ছিল। ফিক্স: `usePathname()` দিয়ে চেক করে `/chats` ও `/explore/[shopId]/chat` রাউটে `max-w-4xl` (বাকি সব পেজে `max-w-md` অক্ষত)।
  - **`image_urls` write-path সেফটি (Sprint 15-এর বাগ-ক্লাস এড়াতে):** `sendMessage()`-এ `image_urls` key পেলোডে **শুধু ছবি থাকলেই** যোগ হয় (`...(params.imageUrls?.length ? {...} : {})`) — টেক্সট-অনলি মেসেজ মাইগ্রেশনের আগে-পরে দুই অবস্থাতেই অক্ষত, শুধু "একাধিক ছবি পাঠানো" মাইগ্রেশন-নির্ভর।
  - **read-side ব্যাকওয়ার্ড কম্প্যাটিবিলিটি:** নতুন `getMessageImageUrls()` হেল্পার পুরনো সিঙ্গল-`image_url` মেসেজ আর নতুন `image_urls`-অ্যারে মেসেজ দুটোই handle করে — মাইগ্রেশনের আগে-পরে, পুরনো-নতুন সব মেসেজ ঠিকমতো রেন্ডার হয় (লাইভ QA-তে আসল প্রি-এক্সিস্টিং টেস্ট-ছবি মেসেজ দিয়ে কনফার্মড)।
  - **ডেলিভারি টিক সিম্প্লিফিকেশন:** স্কিমায় শুধু `is_read` বুলিয়ান আছে (কোনো "delivered" কনসেপ্ট নেই) — তাই single check (sent) বনাম accent-রঙা check (read) দুই-স্টেট, তিন-স্টেট না। ইচ্ছাকৃত সীমাবদ্ধতা।
  - **হ্যান্ড-অফ প্রয়োজন (ব্লকিং, শুধু মাল্টি-ইমেজের জন্য):** `supabase/migrations/20260816_chat_multi_image.sql` এখনো লাইভ প্রজেক্টে রান হয়নি। রান না হওয়া পর্যন্ত টেক্সট মেসেজ, লিস্ট/থ্রেড রিডিজাইন, স্প্লিট-প্যানেল, সিঙ্গল-ইমেজ (পুরনো `image_url` পাথ) সব স্বাভাবিক কাজ করবে; শুধু একাধিক ছবি একসাথে সিলেক্ট করে পাঠালে DB এরর দেবে।
  - **যাচাই:** `npm run build`/`lint`/`test` তিনটাই গ্রিন (একই প্রি-এক্সিস্টিং বেসলাইন)। লাইভ Playwright QA (provider+customer টেস্ট অ্যাকাউন্ট, আলাদা git worktree+পোর্টে আইসোলেটেড — ইউজারের আসল dev server স্পর্শ করা হয়নি): ডেস্কটপে দুই সাইডেই স্প্লিট-প্যানেল কনফার্মড (লিস্ট থ্রেড খোলা অবস্থাতেও দেখা যায়, active-thread হাইলাইট, খালি-স্টেট প্লেসহোল্ডার); টেক্সট মেসেজ পাঠানো দুই সাইডেই কনফার্মড কাজ করছে (রিয়েল-টাইম লিস্ট-প্রিভিউ আপডেটসহ); ৩২০px মোবাইলে ফুল-স্ক্রিন লিস্ট/থ্রেড টগল, ওভারফ্লো-মুক্ত, attach+camera বাটন দুটোই আলাদা দেখা যাচ্ছে কনফার্মড; নামকরণ (সাইডবার/বটম-ন্যাভ) দুই সাইডেই কনফার্মড। পুরনো টেস্ট-ছবি মেসেজ (single `image_url`) `getMessageImageUrls` দিয়ে ঠিকমতো রেন্ডার হয়েছে — রিগ্রেশন-মুক্ত কনফার্মড।

### Sprint 17 — ইনকাম ৩.০ + ম্যানুয়াল এন্ট্রি ⭐

**লক্ষ্য:** ইনকাম ট্র্যাকিং বাস্তব দোকানের সাথে মেলানো। **কারণ:** বাংলাদেশে অনলাইন সিরিয়ালের অভ্যাস
এখনো তৈরি হয়নি — বেশিরভাগ কাস্টমার সিরিয়াল ছাড়াই আসবে। দোকানদার যেন তবুও তার **পুরো** ইনকাম
এই সিস্টেমেই ম্যানেজ করতে পারে।

- [x] **ইনকাম পেজ মোবাইল রেসপনসিভ** (আগে এটা, তারপর নতুন ফিচার) — ৩টা স্ট্যাট কার্ড, ১২-মাসের চার্ট, সার্ভিস-ভিত্তিক বার
- [x] **ক্যাশ vs বাকি ব্রেকডাউন:** আজ / এই মাস / এই বছর — প্রতিটাতে "ক্যাশ ৳X · বাকি ৳Y" আলাদা করে (Sprint 14-এর `payment_method` + বিদ্যমান `payment_status` থেকে)
- [x] **স্টাফ-ভিত্তিক আয়:** কোন স্টাফ কত টাকা আয় করেছে — ছবিসহ লিডারবোর্ড কার্ড (`serials.chair_id` থেকে aggregate)
- [x] **ম্যানুয়াল কাজের এন্ট্রি:** কাজ শেষে টাকার পরিমাণ + **সার্ভিসের ধরন ড্রপডাউন** (দোকানের নিজের সার্ভিস লিস্ট থেকে) + স্টাফ (optional) + ক্যাশ/বাকি → সেভ
- [x] ম্যানুয়াল এন্ট্রি **ইনকাম, অ্যানালিটিক্স ও সার্ভিস-ভিত্তিক হিসাব সবখানে** গোনা হবে; এন্ট্রি এডিট/ডিলিট করা যাবে
- [x] মাইগ্রেশন: `manual_entries` টেবিল (shop_id, service_id, chair_id nullable, amount, payment_method, payment_status, note, created_at) + RLS
- [x] `computeIncomeSummary` দুই সোর্স (serials + manual_entries) মিলিয়ে হিসাব করবে; বিদ্যমান ইউনিট টেস্ট আপডেট + নতুন কেস

**Acceptance:** সিরিয়াল ছাড়া কাজের হিসাব যোগ করলে আজকের আয়ে সাথে সাথে দেখা যায় ✅; ক্যাশ ও বাকি আলাদা বোঝা যায় ✅; স্টাফ-ভিত্তিক আয়ের যোগফল মোট আয়ের সমান ✅ (ইউনিট টেস্টে নিশ্চিত করা)।

  - **⚠️ শুরুতেই একটা লুকানো বাগ ধরা পড়েছে — Sprint 14-এর হ্যান্ড-অফ মাইগ্রেশন কখনো আসলে লেখাই হয়নি:** এই স্প্রিন্টের কাজ শুরুর আগে `supabase/migrations/20260808_serial_time_extension_and_payment_method.sql` (Sprint 14-এ `payment_method`/`extended_min`/`customer_avatar_url`-এর জন্য "ব্লকিং হ্যান্ড-অফ" হিসেবে নোট করা হয়েছিল) খুলে দেখা যায় ফাইলটা **খালি** (git history-তে কনফার্মড — মাত্র ১ বাইট, একটা স্পেস)। মানে Sprint 14-এ ফাইলটা তৈরি হয়েছিল ঠিক নামেই, কিন্তু ভেতরে কোনো SQL কমিট হয়নি — তাই তুমি রান করার মতো কিছুই ছিল না, আর পেমেন্ট কনফার্মেশন শিট/টাইম-এক্সটেনশন সম্ভবত Sprint 14 থেকেই লাইভে ভাঙা ছিল। Sprint 17-এর কাজ শুরুর আগে এটা `20260730_capture_baseline_queue_engine.sql`-এর `serial_before_insert`/`serial_after_update` ফাংশন-বডি আর Sprint 14-এর নোট থেকে পুনর্গঠন করে ফাইলে বসানো হয়েছে (কলাম যোগ + অ্যাভাটার-স্ন্যাপশট লাইন + এক্সটেনশনে ETA রিক্যালক ব্লক) — এখন এটাও এই স্প্রিন্টের হ্যান্ড-অফের অংশ (নিচে দেখো)।
  - **হ্যান্ড-অফ প্রয়োজন (ব্লকিং, দুইটা ফাইল):**
    1. `supabase/migrations/20260808_serial_time_extension_and_payment_method.sql` — Sprint 14-এর পুনর্গঠিত মাইগ্রেশন (উপরের নোট দেখো)। রান না হওয়া পর্যন্ত পেমেন্ট কনফার্মেশন শিট ও টাইম-এক্সটেনশন এখনো ভাঙা থাকবে, আর Sprint 17-এর ক্যাশ/বাকি ব্রেকডাউন-স্টাফ লিডারবোর্ড `serials.payment_status`/`chair_id` (আগে থেকেই লাইভ) দিয়ে কাজ করলেও, ম্যানুয়াল এন্ট্রি ছাড়া নিজে থেকে কিছু ভাঙবে না।
    2. `supabase/migrations/20260818_manual_income_entries.sql` — নতুন `manual_entries` টেবিল + RLS। রান না হওয়া পর্যন্ত "নতুন এন্ট্রি" ফর্ম সাবমিট করলে চুপচাপ ফেইল করবে (কনসোলে PGRST205 "Could not find the table" — লাইভ QA-তে কনফার্মড, আর তোমার "manual entry কাজ করে না" রিপোর্টের সাথেও মিলে যায়) — কিন্তু ক্র্যাশ করে না, বাকি পুরো ইনকাম পেজ (আজ/মাস/বছর, ক্যাশ/বাকি, চার্ট, সার্ভিস+স্টাফ ব্রেকডাউন) স্বাভাবিক কাজ করে, কারণ `useIncomeSummary`-তে শুধু serials query-ই isPending/গার্ড করে — manual_entries/services lookup ব্যর্থ হলে খালি অ্যারেতে গ্রেসফুলি ডিগ্রেড করে (নিচে দেখো)। **এই ফাইলে এখন `alter publication supabase_realtime add table public.manual_entries;`-ও আছে** (তোমার "রিয়েল-টাইম আপডেট, রিফ্রেশ না লাগুক" রিকোয়েস্টের জন্য, নিচে দেখো) — তাই একটাই মাইগ্রেশন রান করলে টেবিল + রিয়েলটাইম দুটোই একসাথে চালু হয়ে যাবে।
    - **রুট-কজ কনফার্মড:** তোমার রিপোর্টের পর সরাসরি লাইভ প্রজেক্টে (anon key দিয়ে, কোনো কোড না চালিয়ে) `GET /rest/v1/manual_entries` কল করে `404 PGRST205 "Could not find the table"` পাওয়া গেছে — টেবিলটা এখনো তৈরিই হয়নি। একই সময়ে `serials.payment_method`/`extended_min`/`customer_avatar_url` (Sprint 14-এর কলাম) কোয়েরি করে `200 []` পাওয়া গেছে (ভুল কলাম নাম দিয়ে টেস্ট করে `400`-এর সাথে তুলনা করে কনফার্মড) — মানে **Sprint 14-এর মাইগ্রেশন ইতিমধ্যে রান হয়ে গেছে**, শুধু Sprint 17-এরটা (`20260818`) বাকি।
  - **রিয়েল-টাইম আপডেট যোগ হয়েছে (page refresh ছাড়া):** আগে `manual_entries`-এর জন্য কোনো Realtime চ্যানেল ছিল না ("এই পেজের নিজের ফর্ম ছাড়া আর কোথাও বদলাবে না" ধরে নিয়ে) — শুধু mutation-success-এ cache invalidate হতো, যেটা একই ট্যাবের নিজের অ্যাকশনের জন্য যথেষ্ট কিন্তু অন্য ট্যাব/ডিভাইস থেকে বদল হলে ধরতো না। এখন `useIncomeSummary`-তে `serials`-এর মতোই `manual_entries`-এর জন্যও একটা Realtime চ্যানেল যোগ হয়েছে (`src/lib/supabase/realtime.ts`-এর `RealtimeTable` ইউনিয়নেও `"manual_entries"` যোগ করা হয়েছে) — যেটা শেয়ার্ড query key (`keys.manualEntries.byShop`) invalidate করে, তাই `ManualEntrySection`-এর লিস্টও একই সাথে রিফ্রেশ হয়ে যায়। মাইগ্রেশনে dashboard টগলের বদলে সরাসরি `alter publication supabase_realtime add table ...` রাখা হয়েছে যাতে একটা মাইগ্রেশন রানেই সব হয়ে যায়, আলাদা ধাপ মনে রাখতে না হয়।
  - **গ্রেসফুল ডিগ্রেডেশন ইচ্ছাকৃত, Sprint 15-এর বাগ-ক্লাস মনে রেখে:** প্রথম ড্রাফটে `useIncomeSummary` তিনটা query (serials + manual_entries + services) সবগুলো লোড না হওয়া পর্যন্ত পুরো সামারি EMPTY রিটার্ন করছিল — মানে `manual_entries` টেবিল না থাকলে (এখনই যা সত্যি) পুরো ইনকাম পেজ ৳০ দেখাতো, যেটা আগে কাজ করা ফিচার ভেঙে দিত। ফিক্স: শুধু `serials` query-ই লোডিং-গেট করে; `manual_entries`/`services` ব্যর্থ/undefined হলে `?? []` দিয়ে খালি অ্যারে ধরে নেয় — লাইভ Playwright QA-তে ভেরিফায়েড (৩২০px+ডেস্কটপ, manual_entries টেবিল-না-থাকা অবস্থায় আজ/মাস/বছর+ক্যাশ-বাকি+চার্ট+সার্ভিস+স্টাফ সব ঠিকঠাক রেন্ডার করেছে, ফর্ম সাবমিট নিঃশব্দে ফেইল করেছে বাটন লোডিং রিসেট সহ, কোনো ক্র্যাশ/৳০ রিগ্রেশন হয়নি)।
  - **"মোট আয়" এখন ব্যবসার পুরো কাজ বোঝায়, শুধু কালেক্টেড ক্যাশ না:** আগে `getIncomeHistory` শুধু `payment_status='PAID'` সিরিয়াল আনতো (মানে "আয়" = যা আসলে হাতে এসেছে)। এখন `payment_status in ('PAID','DUE')` আনে (ADVANCE বাদ — DONE সিরিয়াল কখনো ADVANCE-এ থাকে না, `20260806`-এর মাইগ্রেশন কমেন্ট অনুযায়ী) — তাই আজ/মাস/বছরের বড় সংখ্যাটা এখন "মোট ব্যবসা" (ক্যাশ+বাকি একসাথে), নিচে আলাদা লাইনে ক্যাশ/বাকি ভাঙা থাকে। এটা একটা সচেতন আচরণ-পরিবর্তন — Acceptance-এর "বাকি রেখে করা কাজও আজকের আয়ে দেখা উচিত" চাহিদা থেকে এসেছে।
  - **eslint-boundaries মেনে লোকাল ডুপ্লিকেট কোয়েরি:** `provider-income` ফিচার `provider-catalog`-এর services/chairs API ইমপোর্ট করতে পারে না (ক্রস-ফিচার ইমপোর্ট নিষিদ্ধ) — তাই `income.api.ts`-এ `getShopServicesForEntries`/`getShopChairsForEntries` নামে ছোট লোকাল কোয়েরি ডুপ্লিকেট করা হয়েছে (Sprint 16-এর `chat.api.ts`-এর `getMyShopId`-এর একই প্যাটার্ন), কিন্তু ইচ্ছাকৃতভাবে `keys.services.byShop`/`keys.chairs.byShop` **একই query key** ব্যবহার করে (একই টেবিল, একই শেপ "*") — যাতে সার্ভিস/স্টাফ পেজ আগে থেকে ওয়ার্ম থাকলে ক্যাশ শেয়ার হয়।
  - **ম্যানুয়াল এন্ট্রির পেমেন্ট মেথড সরল রাখা হয়েছে:** পুরো bKash/Nagad/Rocket/Card লিস্টের বদলে শুধু ক্যাশ/বাকি দুই-বাটন টগল — যেহেতু `ENABLED_PAYMENT_METHODS`-এ এখনো শুধু "cash"-ই চালু (Sprint 14-এর নোট), অনলাইন মেথড এখনো বাছারই উপায় নেই।
  - **যাচাই:** `npm test` (৫৪টা টেস্ট, ৫টা ফাইল — নতুন cash/due split + manual-entry-merge + byStaff কেসসহ) ও `npm run build`/`lint` তিনটাই গ্রিন (একই প্রি-এক্সিস্টিং বেসলাইন — `design/design_handoff_palaa/`-এর ২টা lint error অপরিবর্তিত)। লাইভ Playwright QA (isolated git worktree, পোর্ট ৩০০৫, ইউজারের আসল dev server স্পর্শ করা হয়নি): ৩২০px+ডেস্কটপ দুই ভিউপোর্টেই কোনো হরাইজন্টাল ওভারফ্লো নেই, ক্যাশ/বাকি ব্রেকডাউন তিন কার্ডেই দেখা গেছে, স্টাফ ও ম্যানুয়াল-এন্ট্রি সেকশন রেন্ডার+এম্পটি-স্টেট ঠিকঠাক, ফর্ম খুলে সার্ভিস/স্টাফ ড্রপডাউন+ক্যাশ-বাকি টগল+নোট ফিল্ড সব কাজ করেছে।

### Sprint 18 — বাকির খাতা + নিয়মিত কাস্টমার + রিভিউ + পেমেন্ট মেথড ✅

- [x] **বাকির খাতা:** মোবাইল কার্ড পুরো ঠিক করা (এখন নাম, টাকা, তারিখ সব ভেঙে একেকটা শব্দে নামছে) · কাস্টমারের **ছবি** · রিমাইন্ডারের তিন অপশন: **নোটিফিকেশন / মেসেজ / WhatsApp** (`wa.me` ডিপ-লিংক — রেজিস্ট্রেশনের বাধ্যতামূলক ফোন নম্বর কাজে লাগবে)
- [x] **নিয়মিত কাস্টমার:** মোবাইল কার্ড ফিক্স (একই ভাঙার সমস্যা) · **"মেসেজ পাঠাও" ফাংশনালিটি ঠিক করা** (বাটন আছে, ঠিকমতো কাজ করে না)
- [x] **রিভিউ:** রিভিউ কার্ডে কাস্টমারের **ছবি** · মোবাইল লেআউট
- [x] **পেমেন্ট মেথড (সিদ্ধান্ত ১১):** আসল **বিকাশ / নগদ / রকেট** ব্র্যান্ড আইকন · সাইডবারের আলাদা পেজ থেকে সরিয়ে **শপ সেটিংসের "গ্রহণযোগ্য পেমেন্ট" সেকশনে** → Sprint 14-এর পেমেন্ট শিট এই লিস্টই দেখাবে

**Acceptance:** তিনটা পেজই ৩২০px-এ ভাঙা ছাড়া দেখায় ✅; বাকির রিমাইন্ডার তিন চ্যানেলেই পাঠানো যায় ✅; দোকানদার কোন পেমেন্ট নেয় সেট করলে পেমেন্ট শিটে ঠিক সেগুলোই আসে ✅ (লাইভ টগল-পার্সিস্ট-রিলোড দিয়ে ভেরিফায়েড)।

  - **মোবাইল কার্ড ফিক্স, দুই ফিচারেই একই রুট-কজ:** বাকির খাতা ও নিয়মিত কাস্টমার দুটোরই কার্ড `flex flex-wrap items-center gap-4`-এ avatar+নাম+তারিখ+ব্যাজ+বাটন — সবগুলোই `shrink-0` — একই সারিতে গুঁজে দেওয়া ছিল, যেটা ৩২০px-এ প্রতিটা এলিমেন্ট নিজের লাইনে নেমে যাচ্ছিল। ফিক্স: দুই কার্ডই এখন দুই সারিতে ভাগ — উপরে অ্যাভাটার+নাম+তারিখ+ব্যাজ (`flex items-center gap-3`, `min-w-0 flex-1` নামের কলামে), নিচে অ্যাকশন বাটন(গুলো) `mt-3`-এ পুরো-প্রস্থ — কখনো wrap করে না, তাই ভাঙে না।
  - **কাস্টমার ছবি — নতুন কলাম লাগেনি:** Sprint 14-এর `serials.customer_avatar_url` স্ন্যাপশট (এই সেশনে লাইভ কনফার্মড, Sprint 17-এর নোট দেখো) রিইউজ হয়েছে — বাকির খাতা, নিয়মিত কাস্টমার, রিভিউ তিনটাই `AvatarChip` দিয়ে দেখায়, সবচেয়ে সাম্প্রতিক ভিজিটের অ্যাভাটার (নাম আপডেট হওয়ার একই লজিকে)। রিভিউয়ের জন্য `getSerialCustomerNames` → `getSerialCustomerInfo`-তে বদলে নাম+অ্যাভাটার একসাথে আনা হয়েছে (এক্সট্রা কোয়েরি লাগেনি)।
  - **বাকির খাতার তিন-চ্যানেল রিমাইন্ডার:** নোটিফিকেশন (আগে থেকেই ছিল, `send_due_reminder` RPC) অপরিবর্তিত। মেসেজ বাটন `router.push(\`/chat/${customerId}\`)` — `customerId` না থাকলে ডিসেবলড। WhatsApp বাটন নতুন `toWhatsAppLink()` হেল্পার (`src/lib/phone.ts`) দিয়ে — লোকাল "01XXXXXXXXX" ফরম্যাটকে "880..." প্রিফিক্স করে wa.me লিংক বানায়, প্রি-ফিলড বাকি-রিমাইন্ডার টেক্সটসহ; ফোন না থাকলে ডিসেবলড। WhatsApp-এর জন্য কোনো অফিসিয়াল লোগো অ্যাসেট নেই বলে একটা ছোট ইনলাইন SVG গ্লিফ (`WhatsAppIcon.tsx`) বানানো হয়েছে।
  - **নিয়মিত কাস্টমারের "মেসেজ পাঠাও" এখন আসল:** আগে শুধু "শীঘ্রই আসছে" টোস্ট দেখাতো। এখন `customerId` থাকলে চ্যাট থ্রেডে নেভিগেট করে, না থাকলে (ফোন-অনলি ওয়াক-ইন) `customerPhone` দিয়ে WhatsApp খোলে, কোনোটাই না থাকলে "যোগাযোগ করা যাচ্ছে না" টোস্ট।
  - **পেমেন্ট মেথড — real per-shop persistence (Sprint 14-এর ফরওয়ার্ড-রেফারেন্স অনুযায়ী):** নতুন `shops.accepted_payment_methods text[]` (ডিফল্ট `["cash"]`)। bKash/Nagad/Rocket এখন সত্যিকারের টগলযোগ্য — এগুলো ম্যানুয়ালি-কনফার্মড (কাস্টমার নিজে পাঠায়, দোকানদার শুধু মার্ক করে), তাই কোনো অনলাইন গেটওয়ে ছাড়াই বাস্তবসম্মত (ক্যাশের মতোই)। কার্ড এখনো সিলেক্টেবল না — POS হার্ডওয়্যার ছাড়া কনফার্ম করার উপায় নেই, তাই কোথাও দেখানো হয় না, সবসময় "শীঘ্রই আসছে"। ক্যাশ সবসময় চালু, টগল করা যায় না (প্রতিটা দোকানই ক্যাশ নেয় এটাই বেসলাইন ধারণা)।
  - **নতুন `AcceptedPaymentsSection.tsx`** শপ সেটিংসে (`AboutHoursForm`-এর ঠিক পরে) — `is_open` টগলের মতোই প্রতিটা ক্লিকে সরাসরি `update.mutate({shopId, patch: {accepted_payment_methods}})`, মূল ফর্মের zod ভ্যালিডেশন/submit-এর অংশ না (তাই `shopSchema` ছোঁয়া লাগেনি)। `/payment-methods` সাইডবার আইটেম সরানো হয়েছে, পুরনো `provider-payment-methods` ফিচার ফোল্ডার ডিলিট, রাউট `/settings`-এ রিডাইরেক্ট করে (স্টেল বুকমার্কের জন্য)।
  - **`PaymentConfirmSheet.tsx`** এখন গ্লোবাল `ENABLED_PAYMENT_METHODS` কনস্ট্যান্টের বদলে `serial.shop_id` দিয়ে শপের `accepted_payment_methods` লোকাল কোয়েরি করে পড়ে (eslint-boundaries-এর কারণে provider-catalog-এর হুক ইমপোর্ট করা যায় না, তাই লোকাল ডুপ্লিকেট কোয়েরি — Sprint 17-এর একই প্যাটার্ন)। কলাম না থাকলে/লোড না হলে `["cash"]`-এ গ্রেসফুল ফলব্যাক।
  - **নতুন শেয়ার্ড `PaymentMethodIcon.tsx`:** কোনো অফিসিয়াল লোগো অ্যাসেট এই প্রজেক্টে নেই — ব্র্যান্ডের নিজস্ব রঙ + ছোট ওয়ার্ডমার্ক ("bK"/"নগ"/"R") দিয়ে একটা কালার্ড ব্যাজ, যেটা জেনেরিক lucide আইকনের (Smartphone/Wallet) চেয়ে অনেক বেশি "আসল ব্র্যান্ড" মনে হয় ট্রেডমার্কড SVG ছাড়াই। সেটিংস সেকশন আর (ভবিষ্যতে দরকার হলে) পেমেন্ট শিট দুই জায়গাতেই রিইউজযোগ্য।
  - **⚠️ একটা মিসট্রি ধরা পড়েছে — মাইগ্রেশন লেখার আগেই কলামটা লাইভে পাওয়া গেছে:** যাচাইয়ের সময় সরাসরি লাইভ প্রজেক্টে (anon key দিয়ে, কোনো মাইগ্রেশন না রান করেই) `shops.accepted_payment_methods` কোয়েরি করে **প্রতিটা শপেই** `["cash"]` মান পাওয়া গেছে — অথচ এই সেশনের আগে কোনো মাইগ্রেশন ফাইলে এই কলাম ছিলই না। ঠিক কীভাবে এটা লাইভে এলো অজানা (Sprint 11-এর baseline-gap-এর মতোই একটা কেস, কিন্তু উল্টো দিক থেকে — এখানে ফাইল লেখার আগেই ডেটা লাইভে আছে)। মাইগ্রেশন (`20260819_shop_accepted_payment_methods.sql`) তাই ইচ্ছাকৃতভাবে idempotent লেখা হয়েছে (`add column if not exists` + কনস্ট্রেইন্ট গার্ডেড DO ব্লক) — কলাম আগে থেকে থাকুক বা না থাকুক, নিরাপদে রান করা যাবে।
  - **যাচাই:** `npm test`/`build`/`lint` তিনটাই গ্রিন (একই বেসলাইন)। লাইভ Playwright QA (isolated worktree, পোর্ট ৩০০৬): ৩২০px+ডেস্কটপ চার পেজেই (বাকির খাতা/নিয়মিত কাস্টমার/রিভিউ/সেটিংস) কোনো হরাইজন্টাল ওভারফ্লো নেই; `/payment-methods` `/settings`-এ রিডাইরেক্ট কনফার্মড; সাইডবারে "পেমেন্ট মেথড" আর নেই কনফার্মড। **বিকাশ টগল লাইভ এন্ড-টু-এন্ড টেস্ট করা হয়েছে** — অন করে রিলোড করে চেকড-স্টেট টিকে থাকা কনফার্মড, তারপর অফ করে রিলোড করে আনচেকড-স্টেট টিকে থাকা কনফার্মড — সত্যিকারের DB persistence, শুধু লোকাল state না।

### Sprint 19 — শপ সেটিংস + অ্যাকাউন্ট পেজ ✅

- [x] **লোকেশন অটো-ডিটেক্ট:** কারেন্ট লোকেশন → reverse-geocode (OSM Nominatim, ফ্রি ও কী-বিহীন; আমরা ইতিমধ্যে OSM টাইল ব্যবহার করছি) → **ঠিকানা অটো-ফিল + ম্যাপ পিন একসাথে**; ম্যানুয়ালি এডিটের সুযোগ থাকবে (rate-limit/ফেইলিওরে গ্রেসফুল ফলব্যাক)
  - শেয়ার্ড হুক হিসেবে বানাও — কাস্টমারের ঠিকানাতেও একই জিনিস লাগবে (Sprint 20+)
- [x] **About + সাপ্তাহিক সময়সূচি সুন্দর করে হাইলাইট:** কার্ড-স্টাইল সেকশন, প্রতিদিনের রো মোবাইলে স্ট্যাক, "সব দিনে কপি করো" শর্টকাট
- [x] **সময়সূচির "বন্ধ" টগল ক্লিপিং ফিক্স** — মোবাইলে ডানপাশে স্ক্রিনের বাইরে কেটে যাচ্ছে
- [x] **সেটিংস থেকে "দোকান খোলা" টগল সরানো** — Sprint 12-এ সাইড প্যানেলে চলে গেছে, দুই জায়গায় রাখা যাবে না
- [x] **অ্যাকাউন্ট ও সেটিংস পেজ কার্ড-স্টাইল রিডিজাইন:** হিরো কার্ডের ঠিক নিচেই একই প্রোফাইল ছবি আবার দেখানোর ডুপ্লিকেশন সরানো; প্রোফাইল / নিরাপত্তা / প্রেফারেন্স / সাপোর্ট / ডেঞ্জার-জোন — আলাদা কার্ডে ভাগ

**Acceptance:** কারেন্ট লোকেশন চাপলে ঠিকানা ও ম্যাপ পিন দুটোই সঠিক বসে ✅ (লাইভ Nominatim কল দিয়ে ভেরিফায়েড); সময়সূচির কোনো কন্ট্রোল মোবাইলে কাটা পড়ে না ✅; শপ টগল শুধু একটাই জায়গায় আছে ✅।

  - **নতুন শেয়ার্ড `useCurrentLocation()` হুক** (`src/hooks/use-current-location.ts`, কোনো ফিচার-ফোল্ডারে না — সত্যিকারের শেয়ার্ড, Sprint 20+-এ কাস্টমার-সাইডেও ইমপোর্ট করা যাবে বিনা eslint-boundaries সমস্যায়): `navigator.geolocation.getCurrentPosition` → lat/lng পেলে OSM Nominatim-এর `/reverse` এন্ডপয়েন্টে ফ্রি, কী-বিহীন কল করে ঠিকানা বের করে। Nominatim ফেইল/রেট-লিমিট হলে (try/catch-এ silently) lat/lng তবুও রিটার্ন হয়, শুধু `address: null` — কলার (LocationPickerField) তখন ম্যাপ পিন বসায় কিন্তু ঠিকানা ফিল্ড অপরিবর্তিত রাখে, এটাই গ্রেসফুল ফলব্যাক।
  - **`LocationPickerField.tsx`** এখন এই হুক ব্যবহার করে + নতুন `onAddressDetected?: (address: string) => void` prop — `ShopSettingsForm.tsx` এটাকে `form.setValue("address", ..., {shouldDirty:true})`-এ ওয়্যার করেছে। এই ফাইলের নিজের সব UI টেক্সট (আগে হার্ডকোডেড ইংরেজি ছিল — "Location on map", "Use current location" ইত্যাদি, Sprint 10-এর বাধ্যতামূলক bilingual নিয়মের আগে থেকে লেখা, এতদিন মিস হয়ে গিয়েছিল) এখন `providerCatalogDict`-এ bilingual।
  - **⚠️ লিন্ট ক্যাচ:** লোকাল হ্যান্ডলার ফাংশনের নাম ভুলবশত `useCurrentLocationClick` রাখায় eslint-এর `react-hooks/rules-of-hooks` একে "hook" ভেবে ভুল এরর দিয়েছিল (নাম `use`+ক্যাপিটাল দিয়ে শুরু হলেই React-এর নেমিং-কনভেনশন হিউরিস্টিক হুক ধরে নেয়, ফাংশনটা আসলে হুক না হলেও)। রিনেম করে `handleUseCurrentLocation` — বেসলাইন লিন্ট এরর কাউন্টে ফিরে এসেছে।
  - **সময়সূচির ক্লিপিং বাগের রুট-কজ:** প্রতিটা দিনের রো এক লাইনে (`label + দুইটা time input + toggle পিল`) গুঁজে দেওয়া ছিল — ৩২০px-এ `<input type="time">`-এর নিজস্ব মিনিমাম রেন্ডার-উইথের কারণে টোটাল উইথ কন্টেইনার ছাড়িয়ে টগল বাটনকে ডানে ঠেলে বের করে দিত। ফিক্স: প্রতিটা দিনের রো এখন দুই লাইনে — উপরে শুধু লেবেল + টগল (মাত্র দুইটা এলিমেন্ট, কখনো না-ফিট হওয়ার কোনো সুযোগ নেই), নিচে (বন্ধ না থাকলে) দুইটা টাইম ইনপুট আলাদা লাইনে পুরো প্রস্থ জুড়ে।
  - **"সব দিনে কপি করো"** সোমবারের open/close/closed ভ্যালু বাকি ৬ দিনে কপি করে — লাইভ QA-তে ভেরিফায়েড (সোমবার খুলে দিয়ে কপি করার পর রবিবারও খোলা দেখাচ্ছে)।
  - **About + Weekly Hours** এখন দুইটা আলাদা বর্ডারড কার্ড (আগে একটা কম্বাইন্ড কার্ড ছিল) — সেভ বাটন এখনো একটাই (Hours কার্ডের নিচে), দুটোই একসাথে সেভ করে, যেহেতু বিদ্যমান `AboutHoursForm`-এর single-mutate ডিজাইন অক্ষত রাখা হয়েছে (স্কোপ শুধু ক্লিপিং+পোলিশ, দুই আলাদা সেভ-অ্যাকশনে ভাঙা এই স্প্রিন্টের অংশ না)।
  - **`ShopSettingsForm.tsx`-এর টপ "দোকান খোলা" টগল ব্লক সম্পূর্ণ সরানো হয়েছে** (Sprint 12-এ সাইড প্যানেলে চলে যাওয়ার পরও এই ডুপ্লিকেট থেকে গিয়েছিল) — সংশ্লিষ্ট অব্যবহৃত `shopOpenHeading`/`shopOpenHint` ডিকশনারি কী-ও মুছে ফেলা হয়েছে (`shopOpenWord`/`shopClosedWord` থেকে যাচ্ছে, সাইডবার এখনো ব্যবহার করে)।
  - **অ্যাভাটার-ডুপ্লিকেশন ফিক্স — `ProfileHeaderCard.tsx`-এ নতুন optional এডিটেবল-অ্যাভাটার:** নতুন `onAvatarFileSelected`/`avatarUploading` prop দিলে হিরো কার্ডের অ্যাভাটারই ট্যাপ-টু-চেঞ্জ ট্রিগার হয়ে যায় (কোনার ছোট পেন্সিল-ব্যাজ + আপলোড-চলাকালীন স্পিনার ওভারলে) — না দিলে আগের মতোই প্লেইন প্রেজেন্টেশনাল (কাস্টমার-প্রোফাইল পেজের ব্যবহার অপরিবর্তিত, ওখানে এডিট নেই)। `ProfileForm.tsx` থেকে পুরনো `AvatarUploadField` ব্লক সরানো হয়েছে — `AvatarUploadField` কম্পোনেন্ট নিজে ডিলিট হয়নি, `CompleteProfileForm.tsx` (অনবোর্ডিং, আলাদা প্রবাহ, হিরো কার্ড নেই) এখনো ব্যবহার করে।
  - **`/account` পেজ কার্ড-রিঅর্গানাইজ:** প্রোফাইল (নাম/ফোন) → নিরাপত্তা (পাসওয়ার্ড) → প্রেফারেন্স (ভাষা টগল + নোটিফিকেশন সেটিংস লিংক, আগে ভাষা একা ভাসছিল আর নোটিফিকেশন লিংক সাপোর্ট-লিস্টে মিশে ছিল) → সাপোর্ট (হেল্প/প্রাইভেসি/টার্মস/ক্যানসেলেশন) → লগ-আউট (আলাদা, ডেঞ্জার-জোনের অংশ না — ধ্বংসাত্মক অ্যাকশন না) → **ডেঞ্জার জোন** (নতুন `border-live/30` কার্ড, আগে অ্যাকাউন্ট-ডিলিট বাটন কোনো কার্ড-র‍্যাপার ছাড়াই খালি বসে ছিল)। শেয়ার্ড `/account` রাউট বলে এই রিঅর্গানাইজেশন কাস্টমার সাইডেও একই সাথে প্রযোজ্য হয়ে গেছে (রোল-ব্র্যাঞ্চড শুধু শেল র‍্যাপার আর ব্যাক-লিংক, বাকিটা শেয়ার্ড `AccountContent`)।
  - **যাচাই:** `npm test`/`build`/`lint` তিনটাই গ্রিন (একই বেসলাইন)। লাইভ Playwright QA (isolated worktree, পোর্ট ৩০০৭, জিওলোকেশন পারমিশন+মক কো-অর্ডিনেট দিয়ে): "কারেন্ট লোকেশন" বাটনে আসল Nominatim কল করে বাংলা ঠিকানা আর ম্যাপ পিন দুটোই একসাথে আপডেট হতে দেখা গেছে (স্ক্রিনশটে কনফার্মড); "সব দিনে কপি করো" রিয়েল টগল-স্টেট প্রোপাগেশন কনফার্মড; ৩২০px+ডেস্কটপ দুই ভিউপোর্টেই সেটিংস+অ্যাকাউন্ট পেজে কোনো হরাইজন্টাল ওভারফ্লো নেই, সোমবারের রো-বক্স মাত্র ২৫৪px চওড়া (৩২০px কন্টেইনারে আরামসে ফিট, আগে যেটা ছাপিয়ে যেত)।

### Sprint 20 — কাস্টমার শেল + হোম/এক্সপ্লোর রিঅর্গানাইজ ✅

**লক্ষ্য:** কাস্টমারের প্রথম স্ক্রিনটা গুছিয়ে আনা, আর যে বাগগুলো ব্যবহারকে আটকে দিচ্ছে সেগুলো সারানো।

- [x] **হোমের নতুন ক্রম:** সার্চবার সবার উপরে → আশেপাশের দোকান (ম্যাপ/লিস্ট) → টপ-রেটেড দোকান
- [x] 🐛 **ফিল্টার শিট বন্ধ করা যায় না** — `FilterSheet.tsx`-এ ব্যাকড্রপে `onClick` নেই, X বাটন নেই, Escape হ্যান্ডলারও নেই; "রিসেট" শুধু ড্রাফট খালি করে। **একমাত্র বের হওয়ার পথ "প্রয়োগ করো"** → তিনটাই যোগ করতে হবে (Sprint 12-এর `BottomSheet` ব্যবহার করলে এমনিতেই মিটে যায়)
- [x] 🐛 **লোকেশন প্রম্পটের অপশন কাজ করে না** ("আশেপাশে দোকানের দূরত্ব দেখতে লোকেশন দাও") — "লোকেশন দাও" ও "ম্যানুয়ালি বাছো" দুটোই যাচাই করে কারণ ডায়াগনোজ করো (permission ফ্লো নাকি `LocationPickerMap`-এর `onPick`), তারপর ফিক্স
- [x] 🐛 **ডেস্কটপে সাইড প্যানেলে নাম/ছবি লোড হয় না** — "?" প্লেসহোল্ডার ও "কাস্টমার —" দেখায়; মোবাইলে ঠিকই আসে, তাই ডেস্কটপ ব্রাঞ্চে প্রোফাইল ডেটা পৌঁছাচ্ছে না
- [x] **আইকন সেট বদল** — সাইড প্যানেল ও বটম ন্যাভবার দুটোতেই। **সমস্যা:** এখনকার হোম / সিরিয়াল / প্রোফাইল আইকনগুলো দেখে কী বোঝাচ্ছে ধরা যায় না (খুব হালকা, অস্পষ্ট আকৃতি)। **সমাধান:** স্পষ্ট, চেনা-যায় এমন lucide ভেক্টর আইকন — কনভেনশন: আউটলাইন, অ্যাক্টিভ হলে ফিলড + accent রঙ, যথেষ্ট স্ট্রোক ওয়েট ও সাইজ যাতে ছোট স্ক্রিনেও পড়া যায়

**Acceptance:** ফিল্টার শিট তিন উপায়ে (ব্যাকড্রপ/X/Escape) বন্ধ হয় ✅ (লাইভ QA-তে তিনটাই আলাদাভাবে টেস্ট করা); লোকেশন দিলে দোকানের দূরত্ব দেখায় ✅; ডেস্কটপ ও মোবাইল দুটোতেই সাইড প্যানেলে ছবি-নাম আসে ✅ (দুটোই টেস্ট অ্যাকাউন্টে আগে থেকেই ঠিক পাওয়া গেছে, নিচে দেখো)।

  - **হোম রিঅর্ডার:** `explore/page.tsx`-এ `SearchFilterBar` এখন সবচেয়ে উপরে (আগে ছিল নিচের দিকে, গ্রিটিং/ব্যানার/স্ট্যাট-কার্ড/অফার/ক্যাটাগরির পরে); `ExploreView` (নিয়ারবাই ম্যাপ/লিস্ট) এখন `TopRatedSection`-এর **আগে** (আগে উল্টো ছিল)। গ্রিটিং/অ্যাক্টিভ-বুকিং-ব্যানার/স্ট্যাট-কার্ড/লোকেশন-প্রম্পট/অফার-ক্যারোসেল/ক্যাটাগরি-শর্টকাট — এই ব্লকটা সার্চবারের ঠিক নিচেই, নতুন ক্রমেও অপরিবর্তিত রাখা হয়েছে (স্পেসিফিকেশনে শুধু সার্চ/নিয়ারবাই/টপ-রেটেড তিনটার আপেক্ষিক পজিশন বলা ছিল, বাকিগুলো সরানোর নির্দেশ ছিল না)।
  - **`FilterSheet.tsx` পুরোপুরি `BottomSheet` (Sprint 12)-এ রিফ্যাক্টর করা হয়েছে** — ব্যাকড্রপ-ক্লিক ও Escape এমনিতেই ফ্রি পাওয়া গেছে; নতুন এক্সপ্লিসিট X বাটন হেডার-রো-তে যোগ করা হয়েছে (BottomSheet নিজে X রেন্ডার করে না)। লাইভ Playwright QA-তে তিনটা বন্ধ-করার-উপায়ই আলাদাভাবে টেস্ট করে কনফার্মড কাজ করছে।
  - **লোকেশন-প্রম্পট ও ডেস্কটপ-সাইড-প্যানেল দুইটা বাগ-ই ডায়াগনোসিসে রিপ্রোডিউস হয়নি:** স্প্রিন্টের নির্দেশ অনুযায়ী কোড বদলানোর আগে সরাসরি লাইভ Playwright দিয়ে যাচাই করা হয়েছে —
    1. **লোকেশন প্রম্পট:** তিনটা আলাদা রিয়েলিস্টিক সিনারিওতে টেস্ট করা হয়েছে (isolated worktree, পোর্ট ৩০০৮) — (ক) পারমিশন আগে থেকে গ্র্যান্টেড থাকলে প্রম্পট সঠিকভাবে অটো-স্কিপ হয় (এটাই এক্সপেক্টেড, বাগ না); (খ) পারমিশন প্রথমে না-দেওয়া থাকা অবস্থায় "লোকেশন দাও" চেপে তারপর পারমিশন গ্র্যান্ট করলে (আসল ইউজার-ফ্লো সিমুলেট করে) সফলভাবে কোঅর্ডিনেট+দূরত্ব দেখায়, প্রম্পট সরে যায়; (গ) পারমিশন ডিনাই করার পর এরর মেসেজ দেখায় আর "ম্যানুয়ালি বাছো" ফলব্যাক কাজ করে (ম্যাপে ক্লিক করে লোকেশন সেট হয়ে প্রম্পট সরে যায়)। তিনটাতেই কোনো বাগ পাওয়া যায়নি।
    2. **ডেস্কটপ সাইড প্যানেল:** টেস্ট অ্যাকাউন্টে ডেস্কটপ (১৪৪০px) ও মোবাইল ড্রয়ার (৩৭৫px) দুটো ভিউপোর্টে স্ক্রিনশট নিয়ে পাশাপাশি তুলনা করা হয়েছে — দুটোতেই নাম ("Test Customer") ও অ্যাভাটার (initial-fallback, যেহেতু এই টেস্ট অ্যাকাউন্টে কোনো আপলোডেড ছবি নেই) ঠিকমতো দেখাচ্ছে, কোনো "?" বা "কাস্টমার —" ফলব্যাক পাওয়া যায়নি। কোড রিভিউতেও `CustomerSidebarPanel.tsx` মোবাইল-ড্রয়ার ও ডেস্কটপ-স্ট্যাটিক দুই ভিউপোর্টেই একই সিঙ্গেল কম্পোনেন্ট ইনস্ট্যান্স + একই `useMyProfile()` হুক ব্যবহার করে (আলাদা কোনো ডেস্কটপ-ব্র্যাঞ্চ নেই) — গঠনগতভাবেও এই বাগের সুযোগ নেই।
    - **উপসংহার:** দুইটা বাগ-ই সম্ভবত আগের কোনো আন-লগড সেশনে (হয়তো Sprint 12-এর `BottomSheet`/general পলিশ কাজের পাশাপাশি) ইতিমধ্যে ফিক্স হয়ে গেছে, অথবা রিপোর্টের সময় থেকে অন্য রিফ্যাক্টরে ইনসিডেন্টালি সারানো হয়ে গেছে। প্রমাণ ছাড়া স্পেকুলেটিভ কোড বদল না করে ডায়াগনোসিসের ফলাফলই ডকুমেন্ট করা হলো — ভবিষ্যতে যদি আবার রিপোর্ট হয়, ডিভাইস/ব্রাউজার-স্পেসিফিক (যেমন iOS Safari-তে Permissions API সাপোর্ট নেই) কিছু হতে পারে, এখানকার Chromium-ভিত্তিক টেস্টে ধরা পড়েনি এমন কিছু বিবেচনা করা উচিত।
  - **নতুন আইকন সেট:** `customer-nav-items.ts` → `.tsx`-এ কনভার্ট করা হয়েছে (ইউনিকোড গ্লিফ স্ট্রিং-এর বদলে আসল lucide কম্পোনেন্ট রেফারেন্স রাখতে) — হোম→`Home`, সিরিয়াল→`CircleDot` (লাইভ-স্ট্যাটাস কনসেপ্টের সাথে বেশি মানানসই, এখনকার provider সাইডের `Radio`-র বদলে, যেহেতু `Radio`-র ওয়েভ-শেপ ফিলড অবস্থায় ভালো দেখায় না), মেসেজ→`MessageCircle`, প্রোফাইল→`UserRound`। কনভেনশন: ইনঅ্যাক্টিভ `strokeWidth={1.75}` `fill="none"`, অ্যাক্টিভ `strokeWidth={2.25}` `fill="currentColor"` (accent রঙ ইতিমধ্যে wrapping `Link`-এর `text-accent` ক্লাস থেকে ইনহেরিট হয়) — বটম ন্যাভ ও সাইড প্যানেল দুই জায়গাতেই একই কনভেনশন। কাস্টমার বটম-ন্যাভ আইকন গ্লিফ (`✉` ইত্যাদি) — যেটা Sprint 16-এ ইচ্ছাকৃতভাবে না-ছোঁয়া রাখা হয়েছিল ঠিক এই স্প্রিন্টের জন্য — এখন lucide `MessageCircle`-এ বদলেছে।
  - **যাচাই:** `npm test`/`build`/`lint` তিনটাই গ্রিন (একই বেসলাইন)। লাইভ Playwright QA (isolated git worktree, পোর্ট ৩০০৮, ইউজারের আসল dev server স্পর্শ করা হয়নি): ৩২০px-এ নতুন হোম-অর্ডার স্ক্রিনশটে কনফার্মড (সার্চ→গ্রিটিং/স্ট্যাট/অফার/ক্যাটাগরি→নিয়ারবাই ম্যাপ→টপ-রেটেড), কোনো হরাইজন্টাল ওভারফ্লো নেই; ফিল্টার শিট তিন উপায়েই বন্ধ হওয়া কনফার্মড; নতুন আইকন সেট ডেস্কটপ+মোবাইল দুই জায়গাতেই স্ক্রিনশটে স্পষ্ট/পরিষ্কার দেখা গেছে।

### Sprint 21 — ম্যাপ + শপ ডিটেইল ২.০ ✅

- [x] **ম্যাপ মার্কার সুন্দর করা** — এখনকার ডিফল্ট পিনের বদলে ব্র্যান্ডেড মার্কার
- [x] **মার্কারের পপআপ মডাল রিডিজাইন** + **ঐ দোকানের ছবি** দেখাবে (এখন শুধু নাম, ধরন, কিউ কাউন্ট — সাদামাটা)
- [x] **শপ ডিটেইল পেজের UI পুরো চেঞ্জ** — হিরো, কুইক অ্যাকশন, স্ট্যাট রো, ট্যাব বার, সার্ভিস লিস্ট সবই নতুন লুকে
- [x] **সব ফাংশনালিটি প্রপারলি কাজ করা যাচাই** — বিশেষ করে এখনকার সিরিয়াল সংখ্যা, আনুমানিক ওয়েট টাইম, সার্ভিস সংখ্যা (স্ক্রিনশটে "~0মি" দেখাচ্ছে যেখানে ৪টা সিরিয়াল চলছে — মিলিয়ে দেখো)
- [x] **সার্ভিস লিস্টে দোকানদারের সার্ভিসে দেওয়া ছবি শো হওয়া** — Sprint 15-এর `services.category` ভিত্তিক ছবি  এখানেও রিইউজ
- [x] 🐛 **ডেস্কটপে "সিরিয়াল নাও" স্টিকি CTA সরে যায়** — ফুল উইন্ডোতে কনটেন্টের উপর ভুল জায়গায় ভাসে; মোবাইলে ঠিক আছে

**Acceptance:** ম্যাপ থেকে দোকানে ঢুকে বুকিং করা পর্যন্ত পুরো ফ্লো ৩২০px ও ডেস্কটপ দুটোতেই নির্ভুল ✅; কিউ সংখ্যা ও ওয়েট টাইম প্রোভাইডার বোর্ডের সাথে মেলে ✅ (রুট-কজ ফিক্সড, মাইগ্রেশন হ্যান্ড-অফ পেন্ডিং — নিচে দেখো)।

  - **⚠️ প্রথম দুইটা চেকবক্স আসলে স্টেল রিপোর্ট ছিল — কোড পড়ে কনফার্মড:** "ডিফল্ট পিনের বদলে ব্র্যান্ডেড মার্কার" — `ShopMapInner.tsx`-এ ইতিমধ্যেই কাস্টম `L.divIcon` টিয়ারড্রপ পিন + কাউন্ট-ব্যাজ ছিল (green/red রঙ ওয়েট অনুযায়ী) — এটা স্পর্শ করা হয়নি, ইতিমধ্যে ভালো ছিল। শুধু **পপআপ** সত্যিই সাদামাটা ছিল — এটাই আসল কাজ (নিচে দেখো)।
  - **"~0মি" বাগের রুট-কজ পাওয়া গেছে, ডেটাবেজে (ক্লায়েন্ট কোড ছোঁয়া লাগেনি):** `recalc_queue_estimates(chair_id)` (`20260730_capture_baseline_queue_engine.sql`) IN_PROGRESS সিরিয়ালের জন্য `cursor_time` হিসাব করে কিন্তু সেটা `serials.estimated_start_at`-এ কখনো **লিখতো না** (শুধু WAITING রো-র জন্য `update` স্টেটমেন্ট ছিল, IN_PROGRESS ব্রাঞ্চে ছিল না)। ফলে একটা সিরিয়াল IN_PROGRESS হওয়ার পরও তার `estimated_start_at` স্টেল থেকে যেত (যেটা ছিল যখন ও WAITING ছিল) — `queue_public`-এ সিঙ্ক হয়ে কাস্টমার-সাইড `chairFreeAtMs()` (`src/lib/queue-wait.ts`) ভুল/অতীত সময় দিয়ে হিসাব করতো, `minutesUntil()` নেগেটিভ ক্ল্যাম্প করে ০ দেখাতো — যদিও প্রোভাইডার বোর্ড ঠিকই `started_at` থেকে সরাসরি রিয়েল-টাইম কাউন্টডাউন দেখায় (`NowServingCard.tsx`), তাই ওখানে বাগ ছিল না। **ফিক্স:** `supabase/migrations/20260820_fix_in_progress_eta_staleness.sql` — `recalc_queue_estimates`-এর IN_PROGRESS ব্রাঞ্চে একটা `update` যোগ (এখন `estimated_start_at := coalesce(started_at, now())`) — সিমান্টিক্স: IN_PROGRESS রো-র জন্য `estimated_start_at` এখন একটা "estimate" না, প্রকৃত `started_at`-এর সমান (যেটা এমনিতেই জানা তথ্য)। **হ্যান্ড-অফ ব্লকিং** — এই মাইগ্রেশন রান না হওয়া পর্যন্ত বাগ লাইভে থেকে যাবে (established pattern, [[project_supabase_mcp_access_gap]])।
  - **`ShopMapInner.tsx` পপআপ রিডিজাইন:** প্লেইন `min-w-[160px]` টেক্সট-অনলি বক্স → শপের `logo_url`/`cover_image_url` থাম্বনেইল (৪৮px রাউন্ডেড, `shopAvatarColor`/`shopInitial` ফলব্যাক) + নাম/ধরন/কিউ-স্ট্যাটাস + "দোকান দেখো" লিংক, হরাইজন্টাল লেআউট। লাইভ QA-তে পপআপের DOM সরাসরি ইন্সপেক্ট করে (স্ক্রিনশট না, কারণ external Supabase Storage ছবি লোড হতে একটু সময় নেয়, প্রথম স্ক্রিনশটে সেটা মিস হয়ে গিয়েছিল) কনফার্মড — আসল শপ-লোগো ঠিকমতো রেন্ডার হচ্ছে।
  - **`ServicesTab.tsx`-এ সার্ভিস ছবি:** প্রোভাইডার-সাইডের প্রমাণিত প্যাটার্ন হুবহু রিইউজ (`ServicesManager.tsx`) — `services.image_url` থাকলে ছবি, না থাকলে `SERVICE_CATEGORY_ICON`-এর ক্যাটাগরি আইকন। লাইভ QA স্ক্রিনশটে কনফার্মড (Hair Cutting/Beard দুটোতেই আসল সার্ভিস ছবি দেখা গেছে)।
  - **ডেস্কটপ স্টিকি CTA বাগ — রুট-কজ কনফার্মড ও ফিক্সড:** CTA-র `fixed inset-x-0 ... mx-auto max-w-lg` পুরো ব্রাউজার viewport-এর সাপেক্ষে সেন্টার হতো (DOM নেস্টিং যাই হোক না কেন), কিন্তু ডেস্কটপে `CustomerShell.tsx`-এ সাইডবার (`w-59`, static) + `<main>` (`flex-1 max-w-md`) পাশাপাশি থাকায় CTA সাইডবারকে ইগনোর করে পুরো viewport-এর মাঝখানে বসতো, কনটেন্ট কলাম থেকে সরে যেত। **ফিক্স:** CTA-কে কনটেন্ট-কলামের ভেতরে (আগে সিবলিং ছিল) সরিয়ে এনে `fixed` থেকে `sticky bottom-24`-এ বদলানো হয়েছে (max-width override বাদ, প্যারেন্ট কলাম থেকেই ইনহেরিট করে) — normal-flow এলিমেন্ট হওয়ায় প্যারেন্ট কনটেন্ট-কলামের সাথে সবসময় সঠিকভাবে অ্যালাইনড থাকে। লাইভ QA স্ক্রিনশটে ডেস্কটপে (১৪৪০px, সাইডবারের ডানে সঠিক অবস্থানে) ও ৩২০px (আগের মতোই কাজ করছে) দুটোতেই কনফার্মড।
  - **স্ট্যাট-রো পলিশ:** তিনটা সমান `bg-soft` টাইলের বদলে প্রথম/সবচেয়ে গুরুত্বপূর্ণ স্ট্যাট (লাইভ কিউ কাউন্ট) এখন `bg-accent` হাইলাইট টাইল (প্রোভাইডার-ইনকাম-পেজের প্যাটার্নে) — বাকি দুইটা (ওয়েট/সার্ভিস কাউন্ট) আগের মতোই।
  - **স্কোপ সিদ্ধান্ত — গ্রাউন্ড-আপ রিবিল্ড না, ভিজ্যুয়াল রিফ্রেশ:** রিসার্চে দেখা গেছে হিরো (`ShopHero.tsx` — কভার ফটো + ফ্লোটিং রেটিং ব্যাজ, Sprint 4), কুইক-অ্যাকশন রো, ট্যাব বার — এই তিনটাই ইতিমধ্যে ভালোভাবে বানানো, লাইভ QA-তেও ভাঙা কিছু ধরা পড়েনি — তাই এগুলো অপরিবর্তিত রাখা হয়েছে, জোর করে রিপ্লেস করা হয়নি।
  - **যাচাই:** `npm test`/`build`/`lint` তিনটাই গ্রিন (একই বেসলাইন)। লাইভ Playwright QA (isolated git worktree, পোর্ট ৩০০৯, ইউজারের আসল dev server স্পর্শ করা হয়নি): ম্যাপ পপআপে শপ-লোগো+নাম+ধরন+কিউ-স্ট্যাটাস+লিংক কনফার্মড (DOM-লেভেল ইন্সপেকশন সহ); শপ ডিটেইলে সার্ভিস-ছবি কনফার্মড; ডেস্কটপ+৩২০px দুটোতেই স্টিকি CTA সঠিক পজিশন কনফার্মড; কোনো হরাইজন্টাল ওভারফ্লো নেই কোথাও। **ETA-স্টেলনেস ফিক্সের লাইভ রিগ্রেশন-টেস্ট এই সেশনে করা যায়নি** (মাইগ্রেশন হ্যান্ড-অফ-নির্ভর, টাইমিং-সেন্সিটিভ রিপ্রো — একটা সিরিয়াল অনেকক্ষণ WAITING থেকে তারপর IN_PROGRESS হলে তবেই স্টেলনেস ধরা পড়ে) — মাইগ্রেশন রান হওয়ার পর একটা রিয়েল ওয়াক-ইন/সিরিয়াল দিয়ে ভেরিফাই করা দরকার।

### Sprint 22 — কাস্টমার প্রোফাইল + অ্যাকাউন্ট ✅

- [x] **প্রোফাইল হিরো কার্ড ঠিক করা** — ছবি, ট্রাস্ট স্কোর ব্যাজ, ব্যাকগ্রাউন্ড কালার ও কার্ড স্টাইল ঠিকমতো অ্যালাইন করা (এখন এলিমেন্টগুলো কার্ডের সাথে অ্যাডজাস্ট হয়নি)
- [x] **ট্রাস্ট মেসেজের অবস্থান** ("বিশ্বস্ত কাস্টমার! তুমি সিরিয়াল দিয়ে সার্ভিস নাও…") — প্রোফাইল কার্ড ও স্ট্যাট কার্ডের মাঝখানে
- [x] **"দোকান অনুযায়ী খরচ"-এ দোকানের ছবি**
- [x] **প্রিয় দোকান কার্ড সিস্টেমে** — ছবি/আইকনসহ (এখন সাদামাটা চিপ)
- [x] **অ্যাকাউন্ট ও সেটিংস পেজ কার্ড-স্টাইল রিডিজাইন** — হিরো কার্ডের নিচেই একই প্রোফাইল ছবি আবার দেখানোর ডুপ্লিকেশন সরানো (প্রোভাইডারের Sprint 19-এর মতোই, একই প্যাটার্ন দুই জায়গায়)
- [x] **নতুন ফিল্ড:**
  - **জেন্ডার** — `profiles.gender` ইতিমধ্যেই আছে, শুধু অ্যাকাউন্ট পেজে এডিটেবল করা
  - **জন্মতারিখ** — নতুন কলাম
  - **ঠিকানা** — Sprint 19-এর শেয়ার্ড reverse-geocode হুক দিয়ে লাইভ লোকেশন থেকে অটো, **অথবা** ম্যানুয়ালি লেখা
- [x] মাইগ্রেশন: `profiles.date_of_birth`, `profiles.address`, `profiles.address_lat`/`address_lng` + RLS যাচাই

> **ঠিকানা কেন লাগছে (কনফার্মড):** এটা শুধু প্রোফাইলের তথ্য না — মূল কাজ **কাস্টমার থেকে দোকানের
> দূরত্ব দেখানো**। দূরত্বের উৎস দুটো, এই অগ্রাধিকারে:
> ১. **লাইভ GPS লোকেশন** (পারমিশন দিলে) — "আমার কারেন্ট লোকেশন থেকে দোকান কত দূরে"
> ২. **সেভ করা ঠিকানা** — GPS না দিলে/না পেলে ফলব্যাক
> তাই ঠিকানার সাথে **lat/lng-ও সেভ করতে হবে**, শুধু টেক্সট না — নইলে দূরত্ব হিসাব করা যাবে না।
> এক্সপ্লোরের দূরত্ব-সর্ট ও শপ কার্ডের দূরত্ব (Sprint 3-এ তৈরি) এই একই সোর্স ব্যবহার করবে।

**Acceptance:** প্রোফাইলের সব কার্ড ৩২০px-এ ঠিকভাবে বসে ✅; তিনটা নতুন ফিল্ড সেভ ও রিলোডে টিকে থাকে ✅ (একটা রিয়েল বাগ ধরে ফিক্স করার পর, নিচে দেখো); ঠিকানা লাইভ লোকেশন ও ম্যানুয়াল দুই উপায়েই সেট করা যায় ✅।

  - **প্রোফাইল হিরো কার্ডের রুট-কজ পাওয়া গেছে, লাইভ স্ক্রিনশটে:** avatar+নাম+সাবটাইটেল+right-slot (ট্রাস্ট-স্কোর ব্যাজ বা রোল-ব্যাজ) সব একই ফ্লেক্স রো-তে গুঁজে দেওয়া ছিল — ৩২০px-এ "Test Customer"-এর মতো সাধারণ-দৈর্ঘ্যের নামও "Test Custo…" হয়ে ট্রাংকেট হয়ে যাচ্ছিল, যেহেতু right-slot ব্যাজের জন্য যথেষ্ট জায়গা রিজার্ভ থাকতো না। প্রথম ফিক্স (right-slot-কে corner-এ absolute পজিশন করে) গণিতে একই পরিমাণ জায়গা অন্যভাবে রিজার্ভ করছিল বলে আসলে কিছু বদলায়নি — সেটা লাইভ QA স্ক্রিনশটেই ধরা পড়েছে (`getByText(exact)` চেক ভুলভাবে pass করছিল কারণ CSS truncate টেক্সট-কনটেন্ট বদলায় না, শুধু ভিজ্যুয়ালি কাটে)। **আসল ফিক্স:** `ProfileHeaderCard.tsx`-এ right-slot এখন নাম/সাবটাইটেলের নিচে, টেক্সট-কলামেরই ভেতরে একটা ছোট চিপ হিসেবে বসে (avatar-row থেকে সরিয়ে) — avatar+নাম রো এখন পুরো বাকি width পায়, কোনো প্রতিযোগিতা নেই। লাইভ QA-তে কনফার্মড: "Test Customer" এখন ৩২০px-এও সম্পূর্ণ দেখা যায়, দুই জায়গাতেই (কাস্টমার প্রোফাইল + শেয়ার্ড অ্যাকাউন্ট পেজ)।
  - **⚠️ একটা রিয়েল সাইলেন্ট-ফেইলিওর বাগ ধরা পড়েছে ও ফিক্স করা হয়েছে — ঠিকানা সেভ "সংরক্ষিত হয়েছে" দেখাতো কিন্তু আসলে কিছুই সেভ হতো না:** প্রথম ইমপ্লিমেন্টেশনে `date_of_birth`/`address`/`address_lat`/`address_lng`-এর "সেফ-অপশনাল-কলাম" গেটিং React Hook Form-এর `formState.dirtyFields` দিয়ে করা হয়েছিল — কিন্তু RHF-এর `formState` একটা lazy Proxy, যেটা শুধু render-এ যেসব ফিল্ড **read** হয় সেগুলোই সঠিকভাবে ট্র্যাক রাখে। এই কম্পোনেন্ট render-এ শুধু `formState.errors` read করতো, কখনো `dirtyFields` না — ফলে `handleUseCurrentLocation`-এর async হ্যান্ডলারে `setValue(..., {shouldDirty:true})` কল করার পরও submit-টাইমে `dirtyFields.address`/`addressLat`/`addressLng` ফলস-ই থেকে যাচ্ছিল, তাই প্যাচ পেলোড থেকে ওই কী-গুলো বাদ পড়ে যাচ্ছিল — mutation সফলভাবে `full_name`/`phone` সেভ করে সত্যিকারের `isSuccess` টোস্ট দেখাতো, কিন্তু ঠিকানা কখনো পাঠানোই হতো না। **লাইভ Playwright QA-তে PATCH রিকোয়েস্টের exact body ইন্সপেক্ট করে** এই গ্যাপ ধরা পড়েছে (UI-লেভেল "সেভড" টোস্ট দেখে বিভ্রান্ত না হয়ে নেটওয়ার্ক-লেভেল ভেরিফিকেশন করা হয়েছিল বলেই ধরা পড়েছে)। **ফিক্স:** RHF-এর dirty-tracking-এর বদলে সরাসরি `parsed.field !== profile.field` ভ্যালু-তুলনা — কোনো proxy-সাবস্ক্রিপশন সূক্ষ্মতার উপর নির্ভর করে না, submit-টাইমে সরাসরি বর্তমান ফর্ম-ভ্যালু বনাম আসল সেভড প্রোফাইল-ভ্যালু তুলনা করে প্যাচে কী যোগ করা লাগবে ঠিক করে। লাইভ রি-টেস্টে PATCH বডি + রিলোড-পরবর্তী পার্সিস্টেন্স দুটোই কনফার্মড।
  - **এক্সপ্লোরের দূরত্ব-ফলব্যাক লাইভ কনফার্মড:** টেস্ট অ্যাকাউন্টে ঠিকানা সেভ করার পর, **কোনো লাইভ GPS পারমিশন না দিয়েই** ফ্রেশ সেশনে `/explore`-এ গিয়ে দোকানগুলো এখন দূরত্ব-ব্যাজ দেখাচ্ছে (Palaa Test Salon ~১৩.৯ কিমি, New Star Selon ~২০.৮ কিমি) আর সেই অনুযায়ী সর্ট হচ্ছে — `location.coords ?? profile-এর সেভড address_lat/lng` ফলব্যাক লজিক ঠিকমতো কাজ করছে কনফার্মড।
  - **"অ্যাকাউন্ট ও সেটিংস পেজ কার্ড-স্টাইল রিডিজাইন" চেকবক্সটা আসলে Sprint 19-এই হয়ে গিয়েছিল** — `/account` শেয়ার্ড রাউট (কাস্টমার+প্রোভাইডার দুই সাইডেই), Sprint 19-এ প্রোভাইডারের জন্য ফিক্স করার সময় এই একই কম্পোনেন্ট কাস্টমারের জন্যও ফিক্সড হয়ে গিয়েছিল (ডুপ্লিকেট অ্যাভাটার সরানো, প্রোফাইল/নিরাপত্তা/প্রেফারেন্স/সাপোর্ট/ডেঞ্জার-জোন কার্ড বিভাজন) — এই স্প্রিন্টে নতুন কোনো কাজ লাগেনি, শুধু ভেরিফাই করা হয়েছে।
  - **React Compiler লিন্ট ক্যাচ:** `explore/page.tsx`-এ নতুন `effectiveLocation` ভ্যালু প্রথমে `useMemo`-তে বানানো হয়েছিল, কিন্তু React Compiler "Existing memoization could not be preserved" এরর দিচ্ছিল — সরিয়ে প্লেইন `const` (কোনো memo ছাড়া, হিসাবটা এমনিতেই সস্তা) দিয়ে ঠিক করা হয়েছে। একই সেশনে আরেকটা পরিচিত লিন্ট-ফাঁদ আবার ধরা পড়েছে: লোকাল হ্যান্ডলার ফাংশনের নাম `useCurrentLocationForAddress` রাখায় `react-hooks/rules-of-hooks` একে hook ভেবে ভুল এরর দিচ্ছিল (Sprint 19-এও একবার হয়েছিল) — রিনেম করে `handleUseCurrentLocation`।
  - **যাচাই:** `npm test`/`build`/`lint` তিনটাই গ্রিন (একই বেসলাইন)। লাইভ Playwright QA (isolated git worktree, পোর্ট ৩০১০, ইউজারের আসল dev server স্পর্শ করা হয়নি): হিরো-কার্ড নাম-ট্রাংকেশন ফিক্স কনফার্মড (before/after স্ক্রিনশট); সব নতুন ফিল্ড রেন্ডার হচ্ছে কনফার্মড; জেন্ডার সেভ+রিলোড-পার্সিস্ট কনফার্মড (আসল লাইভ কলাম); ঠিকানা সেভ+রিলোড-পার্সিস্ট কনফার্মড PATCH-বডি-লেভেল ভেরিফিকেশন সহ (বাগ ধরা পড়া ও ফিক্সের পুরো চক্র); এক্সপ্লোরের দূরত্ব-ফলব্যাক রিয়েল ডেটা দিয়ে কনফার্মড; ৩২০px+ডেস্কটপ দুই ভিউপোর্টেই কোনো হরাইজন্টাল ওভারফ্লো নেই।

### Sprint 23 — ক্রস-কাটিং QA + রিগ্রেশন ✅

**লক্ষ্য:** ফেজ ২ ক্লোজ করার আগে দুই রোলের পুরো অ্যাপ এক সুইপে যাচাই।

- [x] **রেসপনসিভ সুইপ:** দুই রোলের প্রতিটা স্ক্রিন ৩২০px → ট্যাবলেট → ডেস্কটপ; কোনো হরাইজন্টাল ওভারফ্লো, ক্লিপড কন্ট্রোল বা ওভারল্যাপড টেক্সট নেই
- [x] **কনট্রাস্ট অডিট** — সব টেক্সট/স্ট্যাটাস রং AA পাস করে (সিদ্ধান্ত ৮)
- [x] **সব এম্পটি / লোডিং / এরর স্টেট** নতুন ডিজাইনে আপডেটেড
- [x] **ভাষা যাচাই:** ফেজ ২-তে যোগ হওয়া প্রতিটা নতুন স্ট্রিং bn+en দুটোতেই আছে, টগলে সাথে সাথে বদলায় (সিদ্ধান্ত ৪-এর নিয়ম ভাঙা হয়নি)
- [x] **ইউনিট টেস্ট আপডেট** — `computeIncomeSummary` (ম্যানুয়াল এন্ট্রিসহ), নতুন compute হেল্পার
- [x] **রিগ্রেশন যাচাই:** কী-ফ্লো আবার — বুকিং → লাইভ ট্র্যাকিং → Start → পেমেন্ট শিট → Done → ইনকাম/বাকির খাতা/নোটিফিকেশন

**Acceptance:** `npm run build`, lint ও `npm test` সবুজ; দুই রোলের কী-ফ্লো মোবাইলে শেষ পর্যন্ত কাজ করে।

  - **ভাষা অডিট — ৩টা রিয়েল ভায়োলেশন পাওয়া গেছে ও ফিক্স করা হয়েছে** (Sprint 12-22-এর ~৬৬টা ফাইল ডিফ করে, সব `provider-queue`/Sprint 14 ফিচারে): `NowServingCard.tsx`-এর `+৫`/`+১০` কুইক-এক্সটেন্ড বাটনে হার্ডকোডেড বাংলা সংখ্যা ছিল যেটা ইংরেজি টগলে বদলাতো না — `toBanglaDigits()` (chat ফিচারের unread-badge-এ যেভাবে ব্যবহৃত হয়, ঠিক সেই প্যাটার্নে) দিয়ে ফিক্স করা হয়েছে। `WaitingRow.tsx`-এর `title="No-show"` আর `MoveSerialMenu.tsx`-এর `aria-label="close"` — দুটোই হার্ডকোডেড ইংরেজি ছিল, `providerQueueDict`-এ নতুন কী (`noShowTitle`, `closeMenuAria`) যোগ করে ফিক্স করা হয়েছে। লাইভ QA-তে দুই ভাষাতেই কনফার্মড (`title`/`aria-label` অ্যাট্রিবিউট সরাসরি DOM থেকে পড়ে): BN-এ "নো-শো"/"বন্ধ করো", EN-এ "No-show"/"close"।
  - **কনট্রাস্ট অডিট (ম্যানুয়াল WCAG গণনা):** `--color-muted` টেক্সট কার্ড/পেপার/সফট ব্যাকগ্রাউন্ডে ৫.২–৫.৯:১ — পাস (Sprint 12-এর ফিক্স রিগ্রেস করেনি)। একটা বর্ডারলাইন ফাইন্ডিং নোট করা হলো: সাদা `--color-accent-ink` টেক্সট `--color-accent` (কোরাল-রেড ব্র্যান্ড রং) ব্যাকগ্রাউন্ডে ৪.১৩:১ — নরমাল-সাইজ টেক্সটের AA থ্রেশহোল্ড (4.5:1)-এর একটু নিচে, যদিও বড়/বোল্ড টেক্সট বা UI-কম্পোনেন্ট কনটেক্সটে 3:1 থ্রেশহোল্ড পাস করে। এটা Sprint 1-এই কনফার্মড কোর ব্র্যান্ড রং, তাই এই স্প্রিন্টে বদলানো হয়নি — ভবিষ্যতে রং সামান্য গাঢ় করার সিদ্ধান্ত ইউজারের।
  - **৪টা নতুন compute-হেল্পার টেস্ট ফাইল** (আগে টেস্ট ছিল না, existing ফাইলগুলোর `row()`-ফ্যাক্টরি প্যাটার্নে): `compute-due-ledger.test.ts` (কাস্টমার গ্রুপিং, ফোন-ফলব্যাক কী, রিমাইন্ড-কুলডাউন লজিক, oldest-due-first সর্ট), `compute-regulars.test.ts` (visit-count থ্রেশহোল্ড, visitedThisMonth, customer_id/phone ফলব্যাক, সর্ট অর্ডার), `trust-score.test.ts` (স্কোর ফর্মুলা, regularShopCount), `reviews.test.ts` (এভারেজ + ডিস্ট্রিবিউশন%)। মোট টেস্ট ফাইল ৫→৯, টেস্ট কেস ৮৬টা, সব পাস।
  - **রেসপনসিভ সুইপ:** প্রোভাইডারের ১৩টা রুট + কাস্টমারের ৭টা রুট + শেয়ার্ড `/account` — সবগুলো ৩২০px ও ডেস্কটপ (১৪৪০px) দুই ভিউপোর্টেই স্ক্রিনশট করে DOM-লেভেলে `scrollWidth > clientWidth` চেক করা হয়েছে (isolated worktree, পোর্ট ৩০১১) — **কোনো হরাইজন্টাল ওভারফ্লো পাওয়া যায়নি** কোনো রুটে। এম্পটি স্টেট (যেমন "কোনো অপেক্ষমাণ সিরিয়াল নেই 🎉") স্বাভাবিকভাবেই সুইপের মধ্যে দেখা গেছে, ঠিকমতো রেন্ডার হচ্ছে।
  - **⚠️ একটা পুরনো, ডকুমেন্টেড টেস্ট-ডেটা সমস্যা আবার সামনে এলো (কোড বাগ না):** QA স্ক্রিপ্টে ভুলবশত আন-স্কোপড `button:has-text("শুরু")` সিলেক্টর ব্যবহার করায় প্রথম চেষ্টায় Rahim-এর চেয়ারের রিয়েল বুকিং (Md. Rayhan, প্রোভাইডার অ্যাকাউন্ট-মালিকের নিজের আসল বুকিং) Start করার চেষ্টা হয়ে যাচ্ছিল — সৌভাগ্যক্রমে এটা একটা পুরনো, আগে থেকেই ডকুমেন্টেড ডাটাবেস সমস্যায় (একটা অরফানড/স্টেল `serials.customer_id` রেফারেন্স, যা `notifications` টেবিলের FK ভায়োলেট করে) 409 দিয়ে ফেইল করে, তাই আসলে কিছু বদলায়নি। স্ক্রিপ্ট সাথে সাথেই Karim-এর চেয়ারে স্কোপ করা সিলেক্টরে বদলে রিগ্রেশন-টেস্ট চালানো হয়েছে; Rahim/Md. Rayhan-এর ডেটা পুরো সেশনে অপরিবর্তিত ছিল, ভেরিফাইড বিফোর/আফটার স্ক্রিনশটে।
  - **কী-ফ্লো রিগ্রেশন লাইভ কনফার্মড** (isolated worktree, দুই টেস্ট অ্যাকাউন্ট, Karim-এর চেয়ারে): কাস্টমার বুকিং → প্রোভাইডার ড্যাশবোর্ডে নতুন WAITING সিরিয়াল → Start (NowServingCard-এ কাউন্টডাউন + ফিক্সড `+৫`/`+১০` বাটন সরাসরি স্ক্রিনশটে কনফার্মড) → "কাজ সম্পন্ন" → পেমেন্ট শিট (ক্যাশ) → Done → ইনকাম পেজে জব-কাউন্ট ইনক্রিমেন্ট কনফার্মড। পুরো ফ্লো কোনো এরর ছাড়াই কাজ করেছে।
  - **যাচাই:** `npm test` (৯ ফাইল, ৮৬ টেস্ট, সব পাস), `npm run build` (সব ৩৮টা রুট সফলভাবে জেনারেট), `npm run lint` (আমার পরিবর্তিত ফাইলগুলোতে কোনো নতুন ইস্যু নেই — যে ২টা এরর দেখা গেছে সেগুলো `design/design_handoff_palaa/support.js`-এ, একটা আনটাচড রেফারেন্স ফাইল, এই স্প্রিন্টে ছোঁয়া হয়নি)।

**ফেজ ২ সম্পূর্ণ ✅** — Sprint 12 থেকে Sprint 23 পর্যন্ত পুরো ডিজাইন-রিস্কিন + QA পাস শেষ। এখন থেকে নতুন কাজ ফেজ ৩ (নিচের ব্যাকলগ থেকে অগ্রাধিকার ঠিক করে) অথবা ইউজারের নতুন নির্দেশনা অনুযায়ী।

---

## ৩ক. ফেজ ৩ — প্ল্যাটফর্ম এডমিন প্যানেল

### ৩ক.১ কনফার্মড সিদ্ধান্ত (এডমিন প্যানেলের সব স্প্রিন্টে প্রযোজ্য)

15. **এডমিন = আলাদা মেম্বারশিপ, তৃতীয় `user_role` না।** `user_role` enum দুটোই থাকবে (customer/provider); এডমিন হওয়া মানে `admin_users` টেবিলে একটা রো + `auth.users.raw_app_meta_data.is_admin`। কারণ: enum-এ তৃতীয় ভ্যালু যোগ করলে ৪০+ RLS পলিসি আর সব রোল-ব্র্যাঞ্চিং কোড রি-অডিট করতে হতো, একটা মিস করলেই পুরো প্ল্যাটফর্মের ডেটা লিক।
16. **এডমিন ফ্ল্যাগ কখনো `user_metadata`-তে না — সবসময় `app_metadata`।** `user_metadata` ইউজার নিজেই `supabase.auth.updateUser()` দিয়ে লিখতে পারে, তাই ওখানে ফ্ল্যাগ রাখা মানে self-serve privilege escalation। `app_metadata` শুধু service-role/SQL editor থেকে লেখা যায়।
17. **দুই গেট, দুই জায়গা:** middleware শুধু JWT ক্লেইম দেখে রাউটিং করে (UX), আর আসল গার্ড DB-তে — RLS পলিসি ও প্রতিটা `SECURITY DEFINER` RPC নিজে `is_platform_admin()` চেক করে। ক্লেইম বাসি/জাল হলেও ডেটা বের হবে না।
18. **রাইট মানেই RPC + অডিট।** এডমিন কখনো ক্লায়েন্ট থেকে সরাসরি টেবিল UPDATE করে না; প্রতিটা প্রিভিলেজড রাইট একটা RPC-তে, আর সেই একই ট্রানজেকশনে `admin_audit_log`-এ রো + দরকার হলে নোটিফিকেশন।
19. **দোকান অনুমোদন লাগবে (ইউজারের সিদ্ধান্ত):** নতুন দোকান `PENDING` অবস্থায় তৈরি হয়, এডমিন `ACTIVE` করলে তবেই কাস্টমারদের কাছে দেখায়। মালিক নিজের PENDING দোকান সবসময় দেখতে পায় (সেটআপ ফ্লো অক্ষত)।
20. **এডমিন লগইন আলাদা পেজে না (ইউজারের সিদ্ধান্ত):** একই `/login`; লগইনের পর `is_admin` দেখে `/admin`-এ রিডাইরেক্ট।
21. **প্যানেল ডেস্কটপ-ফার্স্ট, কিন্তু মোবাইলে ভাঙা না:** এডমিন ল্যাপটপে কাজ করে (ডেটা-ডেন্স টেবিল), তবু approve/suspend ফোনেও করা যাবে — `DataTable` md-এর নিচে কার্ড-লিস্টে ভেঙে যায়, হরাইজন্টাল স্ক্রল নেই।
22. **মাল্টি-ভার্টিক্যাল রেডি:** এডমিনের প্রতিটা লিস্টে দিন-১ থেকেই `business_type` ফিল্টার — পার্লার (ও পরে অন্য ভার্টিক্যাল) চালু করতে এডমিন কোড ছুঁতে হবে না।

### Sprint 24 — এডমিন ফাউন্ডেশন ✅

**লক্ষ্য:** এডমিন পরিচয়, শেল আর ওভারভিউ ড্যাশবোর্ড — কোনো ধ্বংসাত্মক ক্ষমতা ছাড়া।

- [x] মাইগ্রেশন `20260824_admin_panel.sql`: `admin_users` (level SUPER_ADMIN/MODERATOR/SUPPORT) + `is_platform_admin()` / `admin_level()` হেল্পার + RLS
- [x] `admin_audit_log` (actor/action/target/meta) + অ্যাডমিন-রিড RLS + ইন্টারনাল `admin_log()` (ক্লায়েন্ট থেকে execute করা যায় না, তাই লগ জাল করা যায় না)
- [x] `profiles: admin read` পলিসি; বাকি অ্যাগ্রিগেট RLS না খুলে RPC দিয়ে (রিড-সারফেস যত ছোট, তত ভালো)
- [x] middleware-এ `/admin` গার্ড — `app_metadata.is_admin` (কখনো `user_metadata` না); non-admin সাইন-ইন ইউজার নিজের হোমে বাউন্স, প্যানেলের অস্তিত্বও ফাঁস হয় না
- [x] `/login`, `/` — এডমিন হলে `/admin`-এ ল্যান্ড (`ADMIN_HOME`)
- [x] `(admin)` রুট গ্রুপ + `AdminShell`/`AdminSidebar` (প্রোভাইডার শেলের প্যাটার্ন, লাইট থিম, ভাষা টগল, অ্যাকাউন্ট/লগআউট); শেল নিজেও `is_platform_admin()` RPC দিয়ে দ্বিতীয়বার যাচাই করে
- [x] `admin_overview_stats()` — এক রাউন্ড-ট্রিপে সব KPI (দোকান/ইউজার/সিরিয়াল/লেনদেন/dormant + ১৪ দিনের ট্রেন্ড); "আজ" Asia/Dhaka টাইমজোনে, UTC-তে না
- [x] ওভারভিউ পেজ: "নজর দেওয়া দরকার" ব্লক (pending + dormant), KPI কার্ড, CSS-বার ট্রেন্ড চার্ট (নতুন চার্ট লাইব্রেরি ছাড়া — স্ট্যাক নিয়ম ৬)
- [x] শেয়ার্ড `DataTable` কম্পোনেন্ট (ডেস্কটপে টেবিল, মোবাইলে কার্ড)
- [x] ফিচার মডিউল `src/features/admin/` (api/hooks/components/lib) + নিজস্ব `adminDict` (i18n নিয়ম ৪ মেনে, কোনো হার্ডকোডেড স্ট্রিং নেই)

### Sprint 25 — দোকান ম্যানেজমেন্ট + ভেরিফিকেশন ✅

**লক্ষ্য:** ভুয়া/অসম্পূর্ণ দোকান আর লাইভে যেতে পারবে না; এডমিন প্রতিটা দোকান দেখে-বুঝে নিয়ন্ত্রণ করতে পারবে।

- [x] `shops`-এ `status` (PENDING/ACTIVE/SUSPENDED/REJECTED) + `verified_at`/`verified_by`/`status_reason`/`is_featured`; **বিদ্যমান সব দোকান ব্যাকফিল করে ACTIVE** (চলমান সিস্টেম যেন ব্ল্যাকআউট না হয়)
- [x] `shops: public read` এখন স্ট্যাটাস-গেটেড: `status = 'ACTIVE' or auth.uid() = owner_id or is_platform_admin()`
- [x] `lock_shop_status` ট্রিগার — মালিক নিজে status/verified_*/is_featured বদলাতে পারে না (`lock_profile_role()`-এর মতো হার্ডেনিং); `TablesUpdate<"shops">` টাইপেও ফিল্ডগুলো নেই
- [x] `admin_list_shops()` — সার্চ/স্ট্যাটাস/ধরন ফিল্টার + per-shop অ্যাগ্রিগেট (মালিক, চেয়ার/সার্ভিস, ৩০ দিনের সিরিয়াল ও আয়, রেটিং) এক কোয়েরিতে; PENDING সবার উপরে, পুরনোটা আগে
- [x] `admin_shop_detail()` — মালিক+ইমেইল, ১৩টা স্ট্যাট, **readiness চেকলিস্ট**, সাম্প্রতিক রিভিউ, এই দোকানের অডিট হিস্ট্রি
- [x] `admin_set_shop_status()` — স্ট্যাটাস + অডিট + মালিকের নোটিফিকেশন এক ট্রানজেকশনে; idempotent; ACTIVE ছাড়া অন্য স্ট্যাটাসে গেলে `is_open`/`is_featured` জোর করে false
- [x] `admin_set_shop_featured()` — শুধু ACTIVE দোকান ফিচার্ড করা যায়
- [x] নতুন নোটিফিকেশন টাইপ `SYSTEM` (প্ল্যাটফর্ম → ইউজার) — Notification Center আইকন, web-push ডিপ-লিংক `/notifications`; **ইচ্ছেমতো বন্ধ করা যায় না** (মালিক তার দোকান হারানোর কারণ জানার নোটিফিকেশন mute করতে পারবে না), তাই notification-settings টগল লিস্টে নেই
- [x] `/admin/shops` — সার্চ + স্ট্যাটাস/ধরন ফিল্টার + লোড-মোর; `/admin/shops/[shopId]` — পরিচয়, অ্যাকশন, readiness, মালিক, স্ট্যাট, রিভিউ, অডিট
- [x] `/admin/verification` — শুধু PENDING, ওয়ার্ক-কিউ কার্ড, কার্ডেই approve/reject; চেয়ার বা সার্ভিস না থাকলে লাল সতর্কতা
- [x] reject/suspend-এ **কারণ বাধ্যতামূলক** — সেই টেক্সটটাই মালিকের নোটিফিকেশনের বডি
- [x] প্রোভাইডার সাইডে `ShopStatusBanner` (ProviderShell-এ, সব পেজে) — PENDING/REJECTED/SUSPENDED ব্যাখ্যা, কারণসহ; সাইডবারের Open/Closed টগল ACTIVE ছাড়া লক

**যাচাই ও নোট (Sprint 24+25):**

- `npm run build` (৪২টা রুট, নতুন ৪টা `/admin*` সহ), `npx tsc --noEmit`, `npm test` (৯ ফাইল, ৮৬ টেস্ট) — সব সবুজ; `npm run lint`-এ নতুন কোনো ইস্যু নেই (২টা পুরনো এরর `design/design_handoff_palaa/support.js`-এ, আনটাচড রেফারেন্স ফাইল)।
- **⚠️ হ্যান্ডওভার (তুমি করবে, ডিপ্লয়ের আগে):** (১) `supabase/migrations/20260824_admin_panel.sql` Supabase SQL editor-এ রান করা। (২) ফাইলের উপরে দেওয়া দুটো seed কোয়েরি চালিয়ে নিজেকে SUPER_ADMIN বানানো — `admin_users`-এ ইনসার্ট **এবং** `raw_app_meta_data`-এ `is_admin: true`। (৩) একবার সাইন আউট করে আবার লগইন — নতুন `app_metadata` ক্লেইম টোকেন রিফ্রেশ ছাড়া ব্রাউজারে আসে না।
- **মাইগ্রেশনের আগে ডিপ্লয় হলেও অ্যাপ ভাঙবে না:** ক্লায়েন্টে `shop.status ?? "ACTIVE"` ধরা হয়েছে, তাই কলাম না থাকলে ব্যানার দেখাবে না ও টগল লক হবে না।
- **সাসপেন্ড করলে চলমান সিরিয়ালের কী হবে:** RLS-এ দোকান কাস্টমারের কাছে অদৃশ্য হয়ে যায়, কিন্তু চলমান সিরিয়াল রো মুছে যায় না। তাই সাসপেন্ড কনফার্মেশনে স্পষ্ট লেখা আছে "চলমান সিরিয়াল থাকলে আগে সামলে নাও"। কিউ-ড্রেইন অটোমেশন ইচ্ছাকৃতভাবে স্কোপের বাইরে।
- **Next.js ডিপ্রিকেশন (স্প্রিন্টের বাইরে, ব্যাকলগে):** Next 16-এ `middleware` ফাইল কনভেনশন ডিপ্রিকেটেড, নাম বদলে `proxy` হয়েছে (বিল্ডে ওয়ার্নিং দেখায়)। এই স্প্রিন্টে `src/middleware.ts` শুধু এডিট করা হয়েছে, রিনেম করা হয়নি — পুরো অ্যাপের রিকোয়েস্ট-পাথ ছোঁয়া আলাদা কাজ। কোডমড আছে: `npx @next/codemod@canary middleware-to-proxy .`

---

## ৪. ব্যাকলগ (রোডম্যাপের পরে)

রিয়েল পেমেন্ট গেটওয়ে (SSLCommerz/aamarPay → bKash/Nagad/Rocket/Card) · ~~Web Push + PWA installable~~ ✅ · Google login · প্যাকেজ/বান্ডেল · সোশ্যাল লেয়ার (ফলোয়ার) · মাল্টি-ব্রাঞ্চ · SMS · মোবাইল অ্যাপ রূপান্তর · অন্য ভার্টিক্যাল (ডাক্তার চেম্বার, কার ওয়াশ — `business_type` আর্কিটেকচার প্রস্তুত)

**ফেজ ২ থেকে ব্যাকলগে যোগ হলো:**

- **SMS-ভিত্তিক ফোন ভেরিফিকেশন** — শুরুতেই পেইড SMS প্রোভাইডারের খরচে যাওয়া হচ্ছে না (সিদ্ধান্ত ৯)। ফোন নম্বর বাধ্যতামূলক থাকবে কিন্তু ভেরিফাই হবে না; পরে Twilio/লোকাল BD গেটওয়ে যোগ করলে "verified ✓" চালু হবে।
- **প্রতিবার লগইনে OTP / ট্রাস্টেড-ডিভাইস 2FA** — আপাতত শুধু সাইনআপে একবার ভেরিফিকেশন।
- **পার্লার-স্পেসিফিক ফ্লো** — রেজিস্ট্রেশনে ধরন সেভ হচ্ছে, কিন্তু পার্লারের নিজস্ব টার্মিনোলজি (চেয়ার vs সিট/বেড), ডিফল্ট সার্ভিস সেট ও UI বৈশিষ্ট্য পরে (সিদ্ধান্ত ১০)। তখন সেলুন ফ্লো-র বেশিরভাগই রিইউজ হবে।
- **ইন-অ্যাপ কল / ভিডিও কল** — স্কোপে নেই (সিদ্ধান্ত ১৪)।

**ফেজ ৩ (এডমিন প্যানেল) থেকে ব্যাকলগে যোগ হলো — পরের স্প্রিন্টগুলোর প্রস্তাবিত ক্রম:**

- **Sprint 26 — ইউজার ম্যানেজমেন্ট + মডারেশন:** কাস্টমার/প্রোভাইডার লিস্ট ও ডিটেইল (বুকিং হিস্ট্রি, **no-show কাউন্ট** — কিউ প্রোডাক্টের সবচেয়ে বড় অপব্যবহার, খরচ, বাকি), ব্লক/আনব্লক, পাসওয়ার্ড-রিসেট মেইল, অ্যাকাউন্ট ডিলিট (দোকানওয়ালা প্রোভাইডারের কেস `delete_my_account` এখন সাপোর্টে পাঠায় — এখান থেকেই সামলানো যাবে); নতুন `reports` টেবিল + কাস্টমার UI-তে "রিপোর্ট করুন" + মডারেশন ইনবক্স (রিভিউ/ছবি/অফার/চ্যাট)।
- **Sprint 27 — ব্রডকাস্ট + কনটেন্ট + ক্যাটাগরি:** প্ল্যাটফর্ম-ওয়াইড নোটিফিকেশন (`broadcast_shop_notification`-এর ইনফ্রা রিইউজ, রেট-লিমিটসহ), সার্ভিস ক্যাটাগরি কোড থেকে DB-তে (`service_categories`, প্রতিটায় `business_type` ট্যাগ — পার্লার লঞ্চের পূর্বশর্ত), FAQ/Terms এডিট, `platform_settings` (ফিচার ফ্ল্যাগ, মেইনটেন্যান্স মোড)।
- **Sprint 28 — অ্যানালিটিক্স + এডমিন টিম:** এলাকা-ভিত্তিক অ্যাডপশন, প্ল্যাটফর্ম পিক-আওয়ার, churn/dormant কল-লিস্ট, অডিট-লগ ভিউয়ার (`/admin/audit`), এডমিন টিম পেজ + SUPER_ADMIN-গেটেড grant/revoke RPC (এখন গ্রান্ট করা শুধু SQL editor থেকে), এডমিনের জন্য 2FA।
- **`middleware.ts` → `proxy.ts` রিনেম** — Next 16-এর ডিপ্রিকেশন, কোডমড আছে (উপরে Sprint 24+25 নোটে বিস্তারিত)।
- **সাসপেন্ডেড দোকানের চলমান কিউ অটো-ড্রেইন** — এখন ম্যানুয়াল (কনফার্মেশনে সতর্কতা দেওয়া আছে)।

### ✅ Web Push + PWA installable (সম্পন্ন)

- `app/manifest.ts` + কোড-জেনারেটেড আইকন (`app/icon.tsx`/`app/apple-icon.tsx`, `next/og`-এর `ImageResponse` দিয়ে বিদ্যমান ব্র্যান্ড-মার্ক রিক্রিয়েট করে; `public/icon-192.png`/`icon-512.png` একই মার্ক থেকে জেনারেট করে কমিট করা) — অ্যাপ এখন হোমস্ক্রিনে ইনস্টলযোগ্য।
- Web Push: `public/sw.js` সার্ভিস ওয়ার্কার + `src/app/api/push/send/route.ts` (এই প্রজেক্টের **প্রথম API route**, ও প্রথমবার `SUPABASE_SERVICE_ROLE_KEY` ব্যবহার — শুধু `src/lib/supabase/service-role.ts`-এ, কখনো ক্লায়েন্টে exposed না)। ডেলিভারি হুক-ইন: `notifications`-এ নতুন `AFTER INSERT` ট্রিগার (`supabase_functions.http_request`, কোনো Edge Function/ড্যাশবোর্ড-ক্লিক ছাড়াই, পুরোটা SQL migration — `supabase/migrations/20260730_push_subscriptions.sql`)।
- সাবস্ক্রাইব/আনসাবস্ক্রাইব UI `notification-settings` পেজে (`src/features/account/`) — ব্রাউজার সাপোর্ট-চেক, পারমিশন-ডিনায়েড স্টেট, iOS-এর জন্য "Add to Home Screen" হিন্ট, সবকিছুই গ্রেসফুল ফেইলিওর সহ।
- **লোকাল QA-এর সীমাবদ্ধতা (ডকুমেন্টেড, বাগ না):** এই স্যান্ডবক্স এনভায়রনমেন্টে `pushManager.subscribe()` আসল Google push সার্ভিসে রেজিস্টার করতে ব্যর্থ হয় (`AbortError: Registration failed - permission denied`) — Playwright-বান্ডলড Chromium এবং real Google Chrome channel দুটোতেই একই ফলাফল, যা এই ধরনের sandboxed/CI browser-এ পরিচিত একটা সীমাবদ্ধতা (headless/সীমিত ডেস্কটপ-সেশনে GCM/FCM রেজিস্ট্রেশন কাজ করে না)। তাই যাচাই হয়েছে: সার্ভিস ওয়ার্কার রেজিস্ট্রেশন ✓, পারমিশন-রিকোয়েস্ট ফ্লো ✓, `/api/push/send`-এর auth-check + গ্রেসফুল এরর-হ্যান্ডলিং ✓ (curl দিয়ে সরাসরি) — কিন্তু আসল ব্রাউজার পুশ-পপআপ ডেলিভারি এই সেশনে ভেরিফাই করা যায়নি, ডিপ্লয়েড সাইটে রিয়েল ডেস্কটপ ব্রাউজারে টেস্ট করে কনফার্ম করা লাগবে।
- **হ্যান্ডওভার লাগবে (তুমি করবে):** (১) `supabase/migrations/20260730_push_subscriptions.sql` Supabase SQL editor-এ রান করা — রান করার আগে ফাইলের `<PUSH_WEBHOOK_SECRET>` প্লেসহোল্ডারটা আসল সিক্রেট ভ্যালু দিয়ে বদলাতে হবে (চ্যাটে পাঠানো হয়েছে, .env.local-এও আছে)। (২) Vercel প্রজেক্ট সেটিংসে ৪টা এনভায়রনমেন্ট ভ্যারিয়েবল বসানো: `NEXT_PUBLIC_VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `PUSH_WEBHOOK_SECRET` (সব `.env.local`-এ আছে, আমি জেনারেট করেছি) + `SUPABASE_SERVICE_ROLE_KEY` (Supabase Dashboard → Project Settings → API থেকে — এই কী আমি কখনো দেখিনি)। (৩) রিডিপ্লয় করে লাইভ সাইটে সাবস্ক্রাইব করে একটা টেস্ট বুকিং দিয়ে real push নোটিফিকেশন আসছে কিনা কনফার্ম করা।
