# QueueFlow — প্রজেক্ট স্পেক

> সংক্ষেপে: এলাকার সেলুন/পার্লারে না গিয়ে বাসা থেকেই সিরিয়াল বুকিং, লাইভ কিউ ট্র্যাকিং, আর দোকানদারের জন্য একটা লাইভ কিউ বোর্ড।

## ১. সমস্যা

এলাকায় অনেকগুলো সেলুন থাকে, কিন্তু কোন দোকানে এখন কত ভীড়/সিরিয়াল চলছে সেটা আগে থেকে জানার উপায় নেই। ফলে না জেনে দোকানে গিয়ে অনেকক্ষণ বসে থাকা লাগে।

## ২. সমাধান — এক নজরে

| রোল | কী দেখে/করে |
|---|---|
| **কাস্টমার** | ম্যাপে আশেপাশের সেলুন/পার্লার দেখে → একটা দোকান বেছে বর্তমান সিরিয়াল সংখ্যা ও এস্টিমেটেড ওয়েট টাইম দেখে → সার্ভিস বেছে বুক করে → বুকিং দিলে নিজের সিরিয়াল নম্বর ও ETA লাইভ ট্র্যাক করে |
| **দোকানদার (provider)** | একটা লাইভ কিউ বোর্ডে দেখে কে অপেক্ষা করছে, কোন চেয়ার/স্টাফে এখন কার কাজ চলছে, কাজ শেষ হলে "Done" করলেই পরের কাস্টমার অটো শুরু হয় |

দুই ধরনের ব্যবসা সাপোর্ট করে — **Salon** (ছেলেদের, চেয়ার/স্টাফ-ভিত্তিক লাইভ কিউ) এবং **Parlour** (মেয়েদের, সার্ভিস-লিস্ট থেকে বেছে অ্যাপয়েন্টমেন্ট)। দুটোই একই বুকিং ফ্লো ব্যবহার করে — parlour-এর জন্য আলাদা কিছু বানাতে হয়নি, `business_type` দিয়েই আলাদা হয়।

## ৩. কাস্টমার ফ্লো (ডিটেইলস)

1. **Explore** (`/explore`) — আশেপাশের খোলা দোকানের লিস্ট, প্রতিটায় লাইভ "N জন অপেক্ষায়" ব্যাজ।
   - *এখন যা নেই, বানানো হচ্ছে:* ম্যাপ ভিউ — দোকানগুলো ম্যাপে পিন আকারে, পিনে ট্যাপ করলে queue count ও শপ ডিটেইলে যাওয়ার শর্টকাট।
2. **Shop detail** (`/explore/[shopId]`) — দোকানের সার্ভিস লিস্ট (নাম, দাম, আনুমানিক সময়) থেকে এক বা একাধিক সার্ভিস বেছে বুক করে।
3. **বুকিং কনফার্ম হলে** সার্ভার সবচেয়ে কম ব্যস্ত চেয়ার/স্টাফ বেছে (`assign_best_chair`) সিরিয়াল বসায়, এবং সেই চেয়ারের বর্তমান ব্যাকলগ থেকে estimated start time হিসাব করে।
4. **My Serial** (`/my-serial`) — নিজের লাইভ সিরিয়াল: সিরিয়াল নম্বর, কতজন আগে আছে, ETA কাউন্টডাউন, কাজ শুরু হয়ে গেলে "In progress" স্টেট। যেকোনো সময় ক্যানসেল করা যায়। একসাথে একটার বেশি অ্যাক্টিভ সিরিয়াল রাখা যায় না (এক দোকানে বুক থাকলে অন্য দোকানে বুক করা যাবে না)।

## ৪. দোকানদার (provider) ফ্লো

1. **Dashboard** (`/dashboard`) — একটা কিউ বোর্ড, প্রতিটা চেয়ার/স্টাফের জন্য একটা কলাম (ট্রেলো-স্টাইল)।
   - প্রতি কলামে: এখন কার কাজ চলছে (in-progress card), তারপর অপেক্ষমান লাইন, আর ব্যাকলগ মিনিট।
   - "Start" চাপলে পরের সিরিয়াল শুরু হয়, "Done" চাপলে শেষ হয়ে পরেরটা auto শুরুর জন্য রেডি হয়।
   - Walk-in (সরাসরি দোকানে আসা কাস্টমার) ম্যানুয়ালি যোগ করার অপশনও আছে।
2. **Chairs** (`/chairs`) — চেয়ার/স্টাফ যোগ, নাম, ছবি, রং, কোন সার্ভিস পারে তা ম্যানেজ।
3. **Services** (`/services`) — সার্ভিস লিস্ট, দাম, আনুমানিক সময় — parlour হলে এটাই মূল জিনিস (সার্ভিস বেছে অ্যাপয়েন্টমেন্ট)।
4. **Settings** (`/settings`) — দোকানের নাম, ঠিকানা, ফোন, ধরন (Salon/Parlour), লোগো/কভার ছবি, Open/Closed টগল।
   - *এখন যা নেই, বানানো হচ্ছে:* ম্যাপে দোকানের অবস্থান (lat/lng) সেট করার UI — DB-তে কলাম আগে থেকেই আছে, ফর্মে ইনপুট নেই।

## ৫. ডেটা মডেল (সংক্ষেপে)

- `profiles` — role: customer/provider
- `shops` — business_type (SALON/PARLOUR), address, latitude/longitude, is_open
- `services` — নাম, দাম, আনুমানিক সময়
- `chairs` — স্টাফ/চেয়ার, কোন সার্ভিস পারে (`chair_service_stats`)
- `serials` — একটা বুকিং: কাস্টমার, সার্ভিসগুলো, চেয়ার, status (WAITING/IN_PROGRESS/DONE/CANCELLED/NO_SHOW), position, estimated_start_at
- `queue_public` — PII ছাড়া পাবলিক ভিউ, explore পেজের লাইভ কাউন্টের জন্য
- DB ফাংশন: `assign_best_chair`, `estimate_duration_on_chair`, `chair_backlog_min` — এস্টিমেশন লজিক সার্ভার-সাইডে (Postgres) হয়, তাই সব ক্লায়েন্টে কনসিস্টেন্ট।

সব রিয়েলটাইম আপডেট Supabase Realtime দিয়ে হয় (`src/lib/supabase/realtime.ts`) — কিউ বোর্ড বা লাইভ ট্র্যাকিং পেজ রিফ্রেশ ছাড়াই আপডেট হয়।

## ৬. বর্তমান অবস্থা

| ফিচার | অবস্থা |
|---|---|
| লগইন/রেজিস্ট্রেশন, রোল-ভিত্তিক রাউটিং | ✅ আছে |
| দোকান সেটআপ (নাম, ধরন, ঠিকানা, খোলা/বন্ধ, ছবি) | ✅ আছে |
| চেয়ার/স্টাফ ও সার্ভিস ম্যানেজমেন্ট | ✅ আছে |
| কাস্টমার এক্সপ্লোর — লিস্ট + লাইভ queue count | ✅ আছে |
| **কাস্টমার এক্সপ্লোর — ম্যাপ ভিউ** | 🔧 এই কাজেই বানানো হচ্ছে |
| সার্ভিস বেছে বুকিং, সেরা চেয়ার অটো-অ্যাসাইন | ✅ আছে |
| লাইভ সিরিয়াল ট্র্যাকিং (ETA, কতজন আগে) | ✅ আছে |
| প্রোভাইডার লাইভ কিউ বোর্ড (চেয়ার-ভিত্তিক কলাম) | ✅ আছে |
| Walk-in সিরিয়াল | ✅ আছে |
| **দোকানের lat/lng সেট করার ফর্ম UI** | 🔧 এই কাজেই বানানো হচ্ছে |
| পার্লার সার্ভিস-লিস্ট অ্যাপয়েন্টমেন্ট | ✅ আছে (একই বুকিং ফ্লো, `business_type: PARLOUR`) |

## ৭. এই মুহূর্তে যা যোগ হচ্ছে

1. দোকান সেটিংস ফর্মে ম্যাপ থেকে লোকেশন পিক/সেভ করার UI।
2. Explore পেজে **ম্যাপ ভিউ** — Leaflet + OpenStreetMap (ফ্রি, কোনো API key লাগে না), প্রতিটা দোকান একটা পিন, পিনে লাইভ queue-count ব্যাজ, ট্যাপ করলে শপ ডিটেইলে যাওয়া যায়। লিস্ট ভিউ এর সাথে টগল করে দেখা যাবে।

## ৮. ভবিষ্যতে বিবেচনার জন্য (এখনই স্কোপে নয়)

- ব্যবহারকারীর বর্তমান লোকেশন থেকে দূরত্ব/নিকটতম সাজানো
- রিভিউ/রেটিং
- SMS/push নোটিফিকেশন ("তোমার সিরিয়াল আসছে")
- একাধিক শাখা (multi-branch) সাপোর্ট
